# jpcite — Windsurf IDE auto-injection shim

> **Source of truth: `AGENTS.md`** at the repo root. Read it first.
> This file exists only so Windsurf's auto-inject picks up a pointer.
> Last canonical edit: 2026-05-17 (Harness H3 — Agent Entry SOT shim).

## See `AGENTS.md`

All project identity, hard constraints, architecture, key commands, quality gates, and "what NOT to do" rules live in `AGENTS.md`. Do not duplicate or override them here.

For live counts (MCP tools / REST routes / OpenAPI paths / program counts) read `scripts/distribution_manifest.yml` or run `python scripts/probe_runtime_distribution.py`.

## Windsurf-specific quirks

- Windsurf's Cascade may attempt to "fix" the `Optional[...]` / Pydantic v1+v2 boundary — leave it alone unless you can prove the change keeps `mypy --strict src/` at 0 errors.
- Default to MCP-first patterns. jpcite value flows via API / MCP / static docs / `.md` companions, not UI.
- For payment plumbing, do **not** bypass the `consent_collection` Stripe footgun — use `custom_text.submit.message` for the ToS link (see `CLAUDE.md` gotchas).
- For search refactor, account for FTS5 trigram single-kanji false-positive overlaps.

## Companion shims

- `AGENTS.md` (root SOT) / `CLAUDE.md` (Claude shim) / `.agent.md` (vendor-neutral shim) / `.cursorrules` (Cursor shim) / `.mcp.json` (MCP client root config).
