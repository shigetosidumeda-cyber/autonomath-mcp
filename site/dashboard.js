(()=>{'use strict';const API_BASE=(typeof window!=='undefined'&&window.JPCITE_API_BASE)||(typeof window!=='undefined'&&window.location&&window.location.hostname==='jpcite.com'?'https://api.jpcite.com':'');const api=(p)=>API_BASE.replace(/\/$/,'')+p;function _readCookie(name){if(typeof document==='undefined'||!document.cookie)return'';const parts=document.cookie.split(';');for(let i=0;i<parts.length;i++){const seg=parts[i].trim();if(!seg)continue;const eq=seg.indexOf('=');const k=eq<0?seg:seg.slice(0,eq);if(k===name){return eq<0?'':decodeURIComponent(seg.slice(eq+1));}}
return'';}
function csrfHeaders(){const tok=_readCookie('am_csrf');const h={'X-Requested-With':'XMLHttpRequest'};if(tok)h['X-CSRF-Token']=tok;return h;}
const DUNNING_DEMOTE_DAILY=100;const DUNNING_STATUSES=new Set(['past_due','unpaid','incomplete']);function isDunningDemoted(me){const s=me&&typeof me.subscription_status==='string'?me.subscription_status.toLowerCase():null;return s!==null&&DUNNING_STATUSES.has(s);}
const QUOTA_REFRESH_MS=60*1000;let _quotaTimer=null;let _lastMe=null;const $=(sel,root=document)=>root.querySelector(sel);const setText=(sel,txt)=>{const el=$(sel);if(el)el.textContent=txt;};const show=(el)=>{if(!el)return;el.removeAttribute('hidden');el.style.display='';};const hide=(el)=>{if(!el)return;el.setAttribute('hidden','');el.style.display='none';};async function fetchJSON(path,opts={}){const init=Object.assign({credentials:'same-origin'},opts);init.headers=Object.assign({'Accept':'application/json'},opts.body?{'Content-Type':'application/json'}:{},opts.headers||{});let _timer=null;if(!init.signal){const ctrl=new AbortController();init.signal=ctrl.signal;_timer=setTimeout(()=>ctrl.abort(),opts.timeoutMs||15000);}
let resp;try{resp=await fetch(api(path),init);}catch(err){if(err&&err.name==='AbortError'){const e=new Error('タイムアウト — 回線が遅いか、サーバが応答していません。');e.status=0;e.timeout=true;throw e;}
throw err;}finally{if(_timer)clearTimeout(_timer);}
let body=null;const text=await resp.text();if(text){try{body=JSON.parse(text);}catch{body=null;}}
if(!resp.ok){const detail=(body&&(body.detail||body.message))||`HTTP ${resp.status}`;const err=new Error(detail);err.status=resp.status;err.body=body;throw err;}
return body;}
function buildScaffold(){const dashSection=document.querySelector('.dash .container');if(!dashSection)return;if(!$('#dash-status')){const bar=document.createElement('div');bar.id='dash-status';bar.style.cssText='margin:0 0 14px;min-height:0;';const anchor=$('.dash .sub')||$('.dash h1');if(anchor&&anchor.parentNode){anchor.parentNode.insertBefore(bar,anchor.nextSibling);}else{dashSection.prepend(bar);}}
if(!$('#dash-signin')){const card=document.createElement('div');card.id='dash-signin';card.hidden=true;card.className='stat-card';card.style.cssText='margin:18px 0 24px;max-width:520px;';card.innerHTML=`
        <p class="stat-label" id="dash-signin-heading">サインイン (永続セッション・推奨)</p>
        <p class="stat-note" id="dash-signin-help" style="margin:0 0 12px;">Stripe または無料トライアルで発行された API キー を貼り付けてください (<code>am_…</code>)。鍵はサーバ側で HMAC 署名 cookie (7 日) に変換され、ブラウザに平文保存されません。即席で 1 回だけ閲覧したい場合は下の <em>利用詳細 (APIキーを貼り付け)</em> セクション (Bearer ヘッダ送信) をどうぞ。</p>
        <form id="dash-signin-form" autocomplete="off" aria-labelledby="dash-signin-heading" aria-describedby="dash-signin-help" style="display:flex;gap:8px;flex-wrap:wrap;">
          <label for="dash-signin-key" class="visually-hidden">API key</label>
          <input id="dash-signin-key" name="api_key" type="password" inputmode="text" required
                 autocomplete="off"
                 placeholder="am_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
                 aria-describedby="dash-signin-help dash-signin-err"
                 style="flex:1;min-width:260px;padding:9px 12px;border:1px solid var(--border);border-radius:6px;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:13px;" />
          <button type="submit" class="btn btn-primary" id="dash-signin-submit">Sign in</button>
        </form>
        <p class="stat-note" id="dash-signin-err" role="alert" aria-live="polite" style="color:var(--danger);margin:10px 0 0;" hidden></p>
      `;const status=$('#dash-status');if(status&&status.parentNode){status.parentNode.insertBefore(card,status.nextSibling);}else{dashSection.appendChild(card);}}
const prelogin=$('#dash-signin');const postElsSet=new Set();const postSelectors=['.dash-nav','#quota-counter','.stat-grid','.chart-card','.keybox','.actions-row',];for(const sel of postSelectors){document.querySelectorAll(sel).forEach((el)=>postElsSet.add(el));}
document.querySelectorAll('.dash .stat-card:not(#dash-signin)').forEach((el)=>{if(!el.closest('.stat-grid'))postElsSet.add(el);});window.__dashPostEls=Array.from(postElsSet);window.__dashPreEl=prelogin;const rotateBtn=document.querySelector('.keybox .btn-danger');const copyBtn=document.querySelector('.keybox .btn-ghost');const manageBilling=Array.from(document.querySelectorAll('.btn')).find((a)=>(a.textContent||'').trim().toLowerCase().startsWith('manage billing'));const logoutLink=Array.from(document.querySelectorAll('.dash-nav a, .dash-nav button')).find((a)=>(a.textContent||'').trim().toLowerCase()==='log out');if(rotateBtn){rotateBtn.id='dash-rotate-btn';rotateBtn.type='button';}
if(copyBtn){copyBtn.id='dash-copy-btn';copyBtn.type='button';}
if(manageBilling)manageBilling.id='dash-billing-btn';if(logoutLink)logoutLink.id='dash-logout-link';}
let _spinCount=0;function withSpinner(label=''){_spinCount+=1;renderStatus();return()=>{_spinCount=Math.max(0,_spinCount-1);renderStatus();};}
let _alertMsg=null;function setAlert(msg){_alertMsg=msg||null;renderStatus();}
function renderStatus(){const bar=$('#dash-status');if(!bar)return;const spin=_spinCount>0?`<span role="status" aria-live="polite" style="display:inline-flex;align-items:center;gap:8px;font-size:13px;color:var(--text-muted);">
           <span style="width:12px;height:12px;border:2px solid var(--border);border-top-color:var(--accent);border-radius:50%;display:inline-block;animation:dash-spin 0.8s linear infinite;"></span>
           読み込み中…
         </span>`:'';const alert=_alertMsg?`<div role="alert" style="margin-top:${spin?'8px':'0'};padding:10px 14px;border:1px solid var(--danger);background:#fff0f0;color:var(--danger);border-radius:6px;font-size:13px;">${escapeHtml(_alertMsg)}</div>`:'';bar.innerHTML=spin+alert;}
function escapeHtml(s){return String(s).replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
if(!document.getElementById('dash-spin-kf')){const st=document.createElement('style');st.id='dash-spin-kf';st.textContent='@keyframes dash-spin { to { transform: rotate(360deg); } }';document.head.appendChild(st);}
function showSignedIn(){hide(window.__dashPreEl);(window.__dashPostEls||[]).forEach(show);}
function showSignedOut(){show(window.__dashPreEl);(window.__dashPostEls||[]).forEach(hide);const sub=document.querySelector('.dash .sub');if(sub)sub.innerHTML='API key を入力してダッシュボードを開いてください。';}
function bind(){const form=$('#dash-signin-form');if(form)form.addEventListener('submit',onSignIn);const rotateBtn=$('#dash-rotate-btn');if(rotateBtn)rotateBtn.addEventListener('click',onRotate);const copyBtn=$('#dash-copy-btn');if(copyBtn)copyBtn.addEventListener('click',onCopyPrefix);const billingBtn=$('#dash-billing-btn');if(billingBtn){billingBtn.addEventListener('click',(e)=>{e.preventDefault();onBillingPortal();});}
const logoutLink=$('#dash-logout-link');if(logoutLink){logoutLink.addEventListener('click',(e)=>{e.preventDefault();onLogout();});}}
async function loadMe(){setAlert(null);const done=withSpinner();try{const me=await fetchJSON('/v1/me');renderMe(me);showSignedIn();loadUsage().catch((e)=>{setAlert(`usage: ${e.message || e}`);});}catch(e){const silentStatuses=new Set([0,401,403,404]);if(silentStatuses.has(e.status)){showSignedOut();}else{setAlert(e.message||'ネットワークエラー');showSignedOut();}}finally{done();}}
function renderMe(me){_lastMe=me;const sub=document.querySelector('.dash .sub');if(sub){const prefix=me.key_hash_prefix||'—';const created=me.created_at?`  ·  since ${String(me.created_at).slice(0,10)}`:'';const cust=me.customer_id?`  ·  ${me.customer_id}`:'';sub.innerHTML=`signed in as <code>${escapeHtml(prefix)}…</code>${escapeHtml(created)}${escapeHtml(cust)}`;}
const hashP=document.querySelector('.keybox .hash-prefix');if(hashP&&me.key_hash_prefix){hashP.textContent=`hash prefix: ${me.key_hash_prefix}`;}
const keyEl=document.querySelector('.keybox .key');if(keyEl){const dots='•'.repeat(28);keyEl.innerHTML=`am_${dots}${escapeHtml(String(me.key_hash_prefix || '').slice(0,4))}`;}
renderBillingCard(me);renderDunningBanner(me);renderPeriodEnd(me&&me.subscription_current_period_end);renderNewKeyReveal(me);}
function renderPeriodEnd(iso){const wrap=document.getElementById('dash-period-end');if(!wrap)return;if(!iso||typeof iso!=='string'){wrap.hidden=true;wrap.style.display='none';return;}
const date=String(iso).slice(0,10);if(!/^\d{4}-\d{2}-\d{2}$/.test(date)){wrap.hidden=true;wrap.style.display='none';return;}
const dateEl=document.getElementById('dash-period-end-date');if(dateEl)dateEl.textContent=date;wrap.hidden=false;wrap.style.display='';}
const DUNNING_COPY=Object.freeze({past_due:'💳 直近のお支払いに失敗しました。Stripe ポータルから支払い方法を更新してください。',unpaid:'⚠️ お支払い未確定です。サービス停止前に Stripe ポータルからご確認ください。',incomplete:'⚠️ お支払い未確定です。サービス停止前に Stripe ポータルからご確認ください。',canceled:'ℹ️ サブスクリプションはキャンセル済みです。当月末まで API アクセス可能です。',});function renderDunningBanner(me){const banner=$('#dash-dunning-banner');if(!banner)return;const status=me&&typeof me.subscription_status==='string'?me.subscription_status.toLowerCase():null;const msg=status&&DUNNING_COPY[status];if(!msg){banner.hidden=true;banner.style.display='none';return;}
const msgEl=$('#dash-dunning-msg');if(msgEl)msgEl.textContent=msg;const portalLink=$('#dash-dunning-portal');if(portalLink){portalLink.onclick=(e)=>{e.preventDefault();onBillingPortal();};}
banner.hidden=false;banner.style.display='block';}
function renderNewKeyReveal(me){const box=$('#dash-newkey-reveal');if(!box)return;let raw=(me&&typeof me.raw_api_key==='string'&&me.raw_api_key)||null;if(!raw){try{raw=sessionStorage.getItem('am_first_key')||null;}catch(_){raw=null;}}
if(!raw){box.hidden=true;box.style.display='none';return;}
const count=me&&typeof me.key_count==='number'?me.key_count:null;const createdAt=me&&me.key_created_at?Date.parse(me.key_created_at):NaN;if(count!==null&&count!==1){}
if(!Number.isNaN(createdAt)){const ageMs=Date.now()-createdAt;if(ageMs>5*60*1000+30*1000){box.hidden=true;box.style.display='none';try{sessionStorage.removeItem('am_first_key');}catch(_){}
return;}}
const valEl=$('#dash-newkey-value');if(valEl)valEl.textContent=raw;const copyBtn=$('#dash-newkey-copy');if(copyBtn){copyBtn.onclick=async()=>{try{await navigator.clipboard.writeText(raw);copyBtn.textContent='Copied!';setTimeout(()=>{copyBtn.textContent='Copy key';},1600);}catch(_){copyBtn.textContent='Copy failed';}};}
const dismissBtn=$('#dash-newkey-dismiss');if(dismissBtn){dismissBtn.onclick=()=>{box.hidden=true;box.style.display='none';try{sessionStorage.removeItem('am_first_key');}catch(_){}};}
box.hidden=false;box.style.display='block';}
function renderBillingCard(me){const billingBtn=$('#dash-billing-btn');if(billingBtn){billingBtn.removeAttribute('aria-disabled');billingBtn.style.opacity='';billingBtn.style.pointerEvents='';billingBtn.textContent='Stripe ポータルを開く →';}
const billingCard=billingBtn?billingBtn.closest('.stat-card'):$('#billing-section');if(!billingCard)return;const val=billingCard.querySelector('[data-billing-headline], .stat-value');if(!val)return;const status=me&&typeof me.subscription_status==='string'?me.subscription_status.toLowerCase():null;let headline;if(status==='active'||status==='trialing'||status===null){headline='¥3 / req <span class="unit">(税込 ¥3.30) — metered, no cap</span>';}else if(DUNNING_STATUSES.has(status)){headline='¥3 / req <span class="unit">(税込 ¥3.30) — payment recovery in progress</span>';}else if(status==='canceled'){headline='¥3 / req <span class="unit">(税込 ¥3.30) — canceled, access ends month-end</span>';}else{headline='¥3 / req <span class="unit">(税込 ¥3.30) — metered</span>';}
val.innerHTML=headline;}
async function loadUsage(){const done=withSpinner();try{const series=await fetchJSON('/v1/me/usage?days=30');const safe=Array.isArray(series)?series:[];renderUsage(safe);renderQuotaCounter(_lastMe,safe);startQuotaTimer();}finally{done();}}
function renderUsage(series){const today=series[series.length-1]||{calls:0,date:''};const last7=series.slice(-7).reduce((s,x)=>s+(x.calls||0),0);const peak=series.reduce((acc,x)=>(x.calls>acc.calls?x:acc),{calls:-1,date:''});const dailyLimit=isDunningDemoted(_lastMe)?DUNNING_DEMOTE_DAILY:0;const todayCard=document.querySelectorAll('.stat-grid .stat-card')[0];if(todayCard){const val=todayCard.querySelector('.stat-value');const fill=todayCard.querySelector('.quota-fill');const note=todayCard.querySelector('.stat-note');if(val){if(dailyLimit>0){val.innerHTML=`${today.calls} <span class="unit">/ ${dailyLimit.toLocaleString()} calls</span>`;}else{val.innerHTML=`${today.calls} <span class="unit">calls (metered)</span>`;}}
if(fill&&dailyLimit>0){const pct=Math.min(100,Math.round((today.calls/dailyLimit)*100));fill.style.width=pct+'%';}else if(fill){fill.style.width='0%';}
if(note){const remaining=Math.max(0,dailyLimit-today.calls);note.textContent=dailyLimit>0?`dunning 緊急枠 (UTC 00:00 reset)。 ${remaining.toLocaleString()} remaining today — restore billing to lift the cap.`:'Metered ¥3/req (税込 ¥3.30) — no daily cap.';}}
const weekCard=document.querySelectorAll('.stat-grid .stat-card')[1];if(weekCard){const val=weekCard.querySelector('.stat-value');const note=weekCard.querySelector('.stat-note');if(val)val.innerHTML=`${last7} <span class="unit">calls</span>`;if(note)note.textContent=`Avg ${Math.round(last7 / 7)} / day.`;}
renderChart(series,peak);renderMonthlyProjection(series);}
function renderMonthlyProjection(series){const el=$('#dash-projection');if(!el)return;if(!Array.isArray(series)||series.length===0){el.textContent='';return;}
const now=new Date();const yyyy=now.getFullYear();const mm=now.getMonth();const dayOfMonth=now.getDate();const totalDays=new Date(yyyy,mm+1,0).getDate();if(dayOfMonth<=2){el.textContent='今月始まりました — 数日後に projection を表示します。';return;}
const monthPrefix=`${yyyy}-${String(mm + 1).padStart(2, '0')}-`;const mtd=series.reduce((sum,x)=>{const d=x&&typeof x.date==='string'?x.date:'';if(d.startsWith(monthPrefix)){return sum+(Number(x.calls)||0);}
return sum;},0);if(mtd<=0){el.textContent=`今月のペース: 〜0 req (まだ呼び出しがありません)`;return;}
const projected=Math.round((mtd/dayOfMonth)*totalDays);const yenCost=Math.round(projected*3);const prevMm=mm===0?11:mm-1;const prevYy=mm===0?yyyy-1:yyyy;const prevPrefix=`${prevYy}-${String(prevMm + 1).padStart(2, '0')}-`;const prevMonthSoFar=series.reduce((sum,x)=>{const d=x&&typeof x.date==='string'?x.date:'';if(d.startsWith(prevPrefix)){const dayPart=parseInt(d.slice(8,10),10);if(Number.isFinite(dayPart)&&dayPart<=dayOfMonth){return sum+(Number(x.calls)||0);}}
return sum;},0);let pctTxt='';if(prevMonthSoFar>0){const pct=Math.round(((mtd-prevMonthSoFar)/prevMonthSoFar)*100);const sign=pct>=0?'+':'';pctTxt=` (前回月初比 ${sign}${pct}%)`;}
el.textContent=`今月のペース: 〜${projected.toLocaleString()} req`+`${pctTxt}`+`  ·  推定 ¥${yenCost.toLocaleString()} (¥3/req)`;}
function _todayCalls(usage){if(!Array.isArray(usage)||usage.length===0)return 0;const todayUTC=new Date().toISOString().slice(0,10);const hit=usage.find((x)=>x&&x.date===todayUTC);if(hit)return Math.max(0,Number(hit.calls)||0);const last=usage[usage.length-1];return Math.max(0,Number((last&&last.calls)||0));}
function renderQuotaCounter(me,usage){const el=$('#quota-counter');if(!el)return;if(!isDunningDemoted(me)){el.setAttribute('data-quota-state','unmetered');hide(el);return;}
const quota=DUNNING_DEMOTE_DAILY;const calls=_todayCalls(usage);const pct=Math.min(100,Math.round((calls/quota)*100));const over=calls>=quota;const state=over?'over':pct>=90?'over':pct>=70?'warn':'ok';const labelEl=el.querySelector('#quota-counter-label');if(labelEl)labelEl.textContent='Dunning 緊急枠 (今日)';const valueEl=el.querySelector('.quota-counter-value');if(valueEl)valueEl.textContent=calls.toLocaleString();const limitEl=el.querySelector('.quota-counter-limit');if(limitEl)limitEl.textContent=quota.toLocaleString();const pctEl=el.querySelector('.quota-counter-pct');if(pctEl){const isEmpty=!Array.isArray(usage)||usage.length===0;if(isEmpty&&calls===0){pctEl.textContent='· ready to go';}else{pctEl.textContent=`· ${pct}% used`;}}
const fill=el.querySelector('.quota-counter-fill');if(fill)fill.style.width=`${pct}%`;const bar=el.querySelector('.quota-counter-bar');if(bar){bar.setAttribute('aria-valuenow',String(pct));bar.setAttribute('aria-label',`Dunning daily quota: ${calls} of ${quota} used (${pct}%)`);}
const note=el.querySelector('.quota-counter-note');if(note){if(over){note.innerHTML='429 — Stripe ポータルから支払い方法を更新すると枠は即時解除されます';}else{note.textContent='reset at 00:00 UTC · restore billing to lift';}}
el.setAttribute('data-quota-state',state);show(el);}
async function refreshQuotaCounter(){if(!_lastMe)return;if(document.visibilityState!=='visible')return;try{const series=await fetchJSON('/v1/me/usage?days=30');renderQuotaCounter(_lastMe,Array.isArray(series)?series:[]);}catch(_){}}
function startQuotaTimer(){if(_quotaTimer!=null)return;_quotaTimer=setInterval(refreshQuotaCounter,QUOTA_REFRESH_MS);if(!window.__dashVisListener){window.__dashVisListener=()=>{if(document.visibilityState==='visible')refreshQuotaCounter();};document.addEventListener('visibilitychange',window.__dashVisListener);}}
function renderChart(series,peak){const svg=document.querySelector('.chart-svg');if(!svg)return;const w=300;const h=80;const n=series.length;const gap=2;const barW=n>0?Math.max(1,Math.floor((w-gap*(n-1))/n)):8;const maxV=Math.max(1,...series.map((x)=>x.calls||0));const maxBarH=70;const baseY=78;const rects=series.map((d,i)=>{const v=d.calls||0;const bh=maxV>0?Math.round((v/maxV)*maxBarH):0;const x=i*(barW+gap);const y=baseY-bh;return`<rect x="${x}" y="${y}" width="${barW}" height="${bh}" fill="#1e3a8a"><title>${escapeHtml(d.date)}: ${v} calls</title></rect>`;}).join('');svg.setAttribute('viewBox',`0 0 ${w} ${h}`);svg.innerHTML=rects;const title=document.querySelector('.chart-title .muted');if(title){if(peak.calls>0){const dateStr=peak.date?peak.date.slice(5):'';title.textContent=`peak ${peak.calls} calls on ${dateStr}`;}else{title.textContent='no calls yet';}}
const axis=document.querySelector('.chart-axis');if(axis&&series.length>=2){const first=series[0].date||'';const last=series[series.length-1].date||'';const fmt=(iso)=>iso.slice(5).replace('-','/');axis.innerHTML=`<span>${escapeHtml(fmt(first))}</span><span>${escapeHtml(fmt(last))}</span>`;}}
async function onSignIn(e){e.preventDefault();setAlert(null);const errEl=$('#dash-signin-err');if(errEl){errEl.hidden=true;errEl.textContent='';}
const input=$('#dash-signin-key');const submit=$('#dash-signin-submit');const key=(input&&input.value||'').trim();if(!key)return;if(submit)submit.disabled=true;const done=withSpinner();try{await fetchJSON('/v1/session',{method:'POST',headers:csrfHeaders(),body:JSON.stringify({api_key:key})});if(input)input.value='';try{if(typeof window.jpciteTrack==='function'){window.jpciteTrack('dashboard_signin_success',{mode:'cookie_session',});}}catch(_e){}
await loadMe();}catch(err){const msg=err.status===429?'サインイン試行回数が多すぎます (1時間あたり最大 5 回)。時間をおいて再試行してください。':err.status===401?'API key が無効、または失効しています。':(err.message||'サインインに失敗しました。');if(errEl){errEl.textContent=msg;errEl.hidden=false;}}finally{done();if(submit)submit.disabled=false;}}
async function onRotate(e){e.preventDefault();const btn=e.currentTarget;if(btn.disabled)return;const ok=window.confirm('API key をローテーションすると、現在の key は即座に失効します。\n'+'新しい key は 1 度だけ表示されます。続けますか？');if(!ok)return;btn.disabled=true;setAlert(null);const done=withSpinner();try{const body=await fetchJSON('/v1/me/rotate-key',{method:'POST',headers:csrfHeaders()});revealNewKey(body.api_key);}catch(err){setAlert(err.message||'rotate に失敗しました。');btn.disabled=false;}finally{done();}}
function revealNewKey(rawKey){try{localStorage.setItem('am_api_key',rawKey);}catch(_){}
try{sessionStorage.removeItem('am_first_key');}catch(_){}
const keybox=document.querySelector('.keybox');if(!keybox)return;const keyEl=keybox.querySelector('.key');if(keyEl){keyEl.textContent=rawKey;keyEl.style.wordBreak='break-all';keyEl.style.whiteSpace='normal';keyEl.style.background='#fff8e6';keyEl.style.borderColor='#f59e0b';}
let warn=keybox.querySelector('.rotate-warning');if(!warn){warn=document.createElement('p');warn.className='rotate-warning stat-note';warn.style.cssText='margin-top:10px;color:#92400e;font-weight:600;';warn.textContent='この key は再表示されません。今すぐ安全な場所に保存してください。';keybox.appendChild(warn);}
const copyBtn=$('#dash-copy-btn');if(copyBtn){copyBtn.textContent='Copy new key';copyBtn.onclick=async()=>{try{await navigator.clipboard.writeText(rawKey);copyBtn.textContent='Copied!';setTimeout(()=>{copyBtn.textContent='Copy new key';},1600);}catch{copyBtn.textContent='Copy failed';}};}}
async function onCopyPrefix(e){e.preventDefault();const btn=e.currentTarget;const prefixText=(document.querySelector('.keybox .hash-prefix')||{}).textContent||'';const m=prefixText.match(/hash prefix:\s*(\S+)/);const val=m?m[1]:'';if(!val)return;try{await navigator.clipboard.writeText(val);const orig=btn.textContent;btn.textContent='Copied!';setTimeout(()=>{btn.textContent=orig;},1400);}catch{btn.textContent='Copy failed';}}
async function onBillingPortal(){const btn=$('#dash-billing-btn');if(btn&&btn.getAttribute('aria-disabled')==='true')return;setAlert(null);const done=withSpinner();try{const body=await fetchJSON('/v1/me/billing-portal',{method:'POST',headers:csrfHeaders()});if(body&&body.url){window.location=body.url;}}catch(err){const body=err&&err.body;const inner=body&&(body.detail&&typeof body.detail==='object'?body.detail:body);if((err.status===404||err.status===400)&&inner&&inner.status==='no_customer'){setAlert(inner.message||'Stripe カスタマーが未作成です。¥3/req の従量課金は使用後に自動作成されます。');}else if(err.status===404||err.status===400){setAlert('Stripe カスタマーが未作成です。¥3/req の従量課金は使用後に自動作成されます。');}else{setAlert(err.message||'billing portal を開けませんでした。');}}finally{done();}}
async function onLogout(){const done=withSpinner();try{await fetchJSON('/v1/session/logout',{method:'POST',headers:csrfHeaders()});}catch(_){}finally{done();window.location.reload();}}
function boot(){buildScaffold();bind();(window.__dashPostEls||[]).forEach(hide);const sub=document.querySelector('.dash .sub');if(sub)sub.textContent='';loadMe();}
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',boot);}else{boot();}})();
