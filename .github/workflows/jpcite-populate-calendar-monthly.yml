name: jpcite-populate-calendar-monthly (alias of populate-calendar-monthly)

# Wave 46 tick7#7 (2026-05-12) — jpcite-brand alias of
# `populate-calendar-monthly.yml`.
# Per `project_jpcite_internal_autonomath_rename` +
# `feedback_destruction_free_organization`: dispatch-only trampoline, no
# cron (parent retains SOT), no logic duplication.

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to run the parent workflow on (default: main)."
        required: false
        default: "main"

concurrency:
  group: jpcite-populate-calendar-monthly
  cancel-in-progress: false

jobs:
  trampoline:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      actions: write
    steps:
      - name: Re-invoke parent (populate-calendar-monthly)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          REF="${{ inputs.ref || 'main' }}"
          echo "alias=jpcite-populate-calendar-monthly parent=populate-calendar-monthly ref=$REF"
          gh workflow run populate-calendar-monthly.yml --ref "$REF" \
            -R "${GITHUB_REPOSITORY}"
