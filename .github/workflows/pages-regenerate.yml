# Generates public program/prefecture/llms/sitemap artifacts from the production
# SQLite registry, then deploys the prepared static artifact directly to
# Cloudflare Pages. Generated pages stay gitignored to avoid committing hundreds
# of megabytes of static HTML back to the repo.
#
# Why we generate static HTML instead of generating at request time:
#   - jpcite.com is a static site on Cloudflare Pages; no server runtime.
#   - Per-program pages are the SEO/GEO surface area (≈10k rows). Crawlers
#     need addressable URLs with prerendered JSON-LD, not SPA placeholders.
#   - Idempotent generator (`_write_if_changed`) keeps deploy diffs stable.
#
# Required GitHub repository secrets:
#   FLY_API_TOKEN  Fly.io deploy token with ssh+sftp on app `autonomath-api`.
#                  Already used by nightly-backup / data-integrity workflows.
#                  Used here to pull the live `data/jpintel.db` from the Fly
#                  volume (it is gitignored, so CI cannot read it from the
#                  checkout).
#
# Optional:
#   CF_API_TOKEN          Cloudflare API token with Pages edit scope.
#   CF_ACCOUNT_ID         Cloudflare account ID.
#
# Runtime budget: ≈10k rows × ~50ms render + sitemap ≈ 8-12 minutes wall.
# `timeout-minutes: 30` leaves slack for sftp + direct Pages deploy.

name: pages-regenerate

on:
  schedule:
    # 19:17 UTC == 04:17 JST. Runs *after* nightly-backup (18:17 UTC) and
    # ingest-daily (19:00 UTC) so the DB we pull already reflects the night's
    # ingest + URL refresh activity.
    - cron: "17 19 * * *"
  workflow_dispatch: {}
  push:
    branches: [main]
    paths:
      - "site/_templates/**"
      - "scripts/generate_program_pages.py"
      - "scripts/generate_prefecture_pages.py"
      - "scripts/generate_geo_industry_pages.py"
      - "scripts/generate_cross_hub_pages.py"
      - "scripts/generate_geo_program_pages.py"
      - "scripts/generate_industry_hub_pages.py"
      - "scripts/generate_industry_program_pages.py"
      - "scripts/generate_compare_pages.py"
      - "scripts/generate_public_counts.py"
      - "scripts/generate_og_images.py"
      - "scripts/regen_llms_full.py"
      - "scripts/regen_llms_full_en.py"
      - "scripts/regen_structured_sitemap_and_llms_meta.py"
      - "scripts/sitemap_gen.py"
      - ".github/workflows/pages-regenerate.yml"

concurrency:
  group: cf-pages-autonomath-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  deployments: write

jobs:
  regenerate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 1

      - name: Check Fly token
        id: fly-token
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          if [ -z "${FLY_API_TOKEN:-}" ]; then
            echo "::notice::FLY_API_TOKEN is not configured; skipping GitHub-hosted page regeneration."
            echo "available=false" >> "$GITHUB_OUTPUT"
          else
            echo "available=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Python
        if: steps.fly-token.outputs.available == 'true'
        uses: actions/setup-python@42375524e23c412d93fb67b49958b491fce71c38 # v5.4.0
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install docs and site dependencies
        if: steps.fly-token.outputs.available == 'true'
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          pip install -r docs-requirements.txt
          pip install -e ".[site]"

      - name: Install CJK fonts for OG image renderer
        if: steps.fly-token.outputs.available == 'true'
        run: |
          set -euo pipefail
          # generate_og_images.py needs a CJK font for 補助金/制度/採択 glyphs.
          # fonts-noto-cjk lays them down at /usr/share/fonts/opentype/noto/.
          # fonts-dejavu-core ships DejaVuSans.ttf for the EN fallback path.
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends fonts-noto-cjk fonts-dejavu-core

      - name: Install flyctl
        if: steps.fly-token.outputs.available == 'true'
        uses: superfly/flyctl-actions/setup-flyctl@ed8efb33836e8b2096c7fd3ba1c8afe303ebbff1 # master

      - name: Pull latest jpintel.db off Fly volume
        if: steps.fly-token.outputs.available == 'true'
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p data
          # Online SQLite backup → /data/jpintel.live.db on the machine, then
          # sftp it down. We avoid copying the live WAL'd file directly.
          remote_db="/data/jpintel.live.${GITHUB_RUN_ID}.${GITHUB_RUN_ATTEMPT}.db"
          local_db="$(basename "$remote_db")"
          cleanup_remote_db() {
            flyctl ssh console -a autonomath-api -C "rm -f ${remote_db}" || true
          }
          trap cleanup_remote_db EXIT
          for attempt in 1 2 3 4 5; do
            echo "::group::Fly jpintel.db backup attempt ${attempt}/5"
            rm -f "$local_db" data/jpintel.db
            if flyctl ssh console -a autonomath-api -C \
                "bash -lc 'set -euo pipefail; rm -f ${remote_db}; sqlite3 -cmd \".timeout 300000\" /data/jpintel.db \".backup ${remote_db}\"; sqlite3 ${remote_db} \"PRAGMA quick_check; SELECT COUNT(*) FROM programs;\"'" \
              && flyctl ssh sftp get "$remote_db" -a autonomath-api ; then
              mv "$local_db" data/jpintel.db
              echo "::endgroup::"
              break
            fi
            echo "::endgroup::"
            if [ "$attempt" -eq 5 ]; then
              echo "::error::Unable to snapshot /data/jpintel.db after 5 attempts."
              exit 1
            fi
            sleep_seconds=$((attempt * 30))
            echo "::warning::jpintel.db snapshot attempt ${attempt} failed; retrying in ${sleep_seconds}s."
            sleep "$sleep_seconds"
          done
          ls -la data/jpintel.db

      - name: Regenerate generated public artifacts
        if: steps.fly-token.outputs.available == 'true'
        run: |
          set -euo pipefail
          python scripts/generate_program_pages.py \
            --db data/jpintel.db \
            --out site/programs \
            --domain jpcite.com \
            --sitemap site/sitemap-programs.xml \
            --tiers S,A,B,C \
            --verbose
          python scripts/generate_prefecture_pages.py \
            --db data/jpintel.db \
            --out site/prefectures \
            --domain jpcite.com \
            --sitemap site/sitemap-prefectures.xml
          python scripts/generate_geo_industry_pages.py \
            --db data/jpintel.db \
            --out site/audiences \
            --domain jpcite.com \
            --sitemap site/sitemap-audiences.xml
          # Hub generators MUST run before detail generators that share a
          # sitemap namespace. cross_hub owns sitemap-cross.xml (47 URLs);
          # geo_program writes the disjoint sitemap-cross-detail.xml.
          # Same pattern for industries (sitemap-industries.xml hubs vs.
          # sitemap-industries-detail.xml details). See
          # analysis_wave18/research_W2_1_generator_audit_2026-05-04.md §4.
          python scripts/generate_cross_hub_pages.py \
            --db data/jpintel.db \
            --out site/cross \
            --domain jpcite.com \
            --sitemap site/sitemap-cross.xml
          python scripts/generate_geo_program_pages.py \
            --db data/jpintel.db \
            --out site/cross \
            --domain jpcite.com \
            --top 50 \
            --sitemap site/sitemap-cross-detail.xml
          python scripts/generate_industry_hub_pages.py \
            --db data/jpintel.db \
            --out site/industries \
            --domain jpcite.com \
            --sitemap site/sitemap-industries.xml
          python scripts/generate_industry_program_pages.py \
            --db data/jpintel.db \
            --out site/industries \
            --domain jpcite.com \
            --sitemap site/sitemap-industries-detail.xml \
            --sitemap-index site/sitemap-index.xml
          python scripts/generate_compare_pages.py
          python scripts/generate_og_images.py --out-dir site/assets
          python scripts/generate_public_counts.py
          python scripts/regen_llms_full.py --db data/jpintel.db --out site/llms-full.txt
          python scripts/regen_llms_full_en.py --db data/jpintel.db --out site/llms-full.en.txt
          python scripts/regen_structured_sitemap_and_llms_meta.py

      - name: Build docs and sitemap
        if: steps.fly-token.outputs.available == 'true'
        run: |
          set -euo pipefail
          mkdocs build --strict
          python scripts/sitemap_gen.py --site-dir site --domain jpcite.com

      - name: GEO readiness guard
        if: steps.fly-token.outputs.available == 'true'
        env:
          PYTHONDONTWRITEBYTECODE: "1"
        run: |
          set -euo pipefail
          python scripts/check_geo_readiness.py

      - name: Prepare public artifact
        if: steps.fly-token.outputs.available == 'true'
        run: |
          set -euo pipefail
          rm -rf dist/site
          mkdir -p dist
          # site/structured/ JSON-LD shards retired 2026-05-03; the public
          # sitemap-structured.xml shard is regenerated above for crawler
          # discovery before sitemap-index.xml is assembled.
          # The CF Pages 20k-file limit no longer requires the old
          # site/structured/ --exclude trick.
          rsync -a --delete \
            --exclude '_templates/' \
            --exclude '*.src.js' \
            --exclude '*.src.css' \
            --exclude '*.map' \
            --include 'press/*.md' \
            --include 'security/policy.md' \
            --include 'laws/index.html' \
            --include 'cases/index.html' \
            --include 'enforcement/index.html' \
            --exclude 'laws/*.html' \
            --exclude 'cases/*.html' \
            --exclude 'enforcement/*.html' \
            --exclude '*.md' \
            site/ dist/site/
          file_count="$(find dist/site -type f | wc -l | tr -d ' ')"
          echo "file count: ${file_count}"
          if [ "$file_count" -gt 19000 ]; then
            echo "::error::Cloudflare Pages artifact has ${file_count} files; limit is 20,000."
            exit 1
          fi

      - name: Check Cloudflare Pages secrets
        id: cf-secrets
        if: steps.fly-token.outputs.available == 'true'
        env:
          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          if [ -z "${CF_API_TOKEN:-}" ] || [ -z "${CF_ACCOUNT_ID:-}" ]; then
            echo "::notice::Cloudflare Pages secrets are not configured; skipping scheduled Pages publish."
            echo "available=false" >> "$GITHUB_OUTPUT"
          else
            echo "available=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish regenerated artifact to Cloudflare Pages
        if: steps.cf-secrets.outputs.available == 'true'
        uses: cloudflare/pages-action@f0a1cd58cd66095dee69bfa18fa5efd1dde93bca # v1.5.0
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          projectName: autonomath
          directory: dist/site
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          branch: main

      - name: Sentry alert on pages-regenerate failure
        if: failure()
        env:
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
        run: |
          set -euo pipefail
          if [ -z "${SENTRY_DSN:-}" ]; then
            echo "::warning::SENTRY_DSN not set; skipping alert hop."
            exit 0
          fi
          PROJECT_ID="$(echo "$SENTRY_DSN" | sed -E 's#.*/([0-9]+)$#\1#')"
          PUBLIC_KEY="$(echo "$SENTRY_DSN" | sed -E 's#https://([^@]+)@.*#\1#')"
          HOST="$(echo "$SENTRY_DSN" | sed -E 's#https://[^@]+@([^/]+)/.*#\1#')"
          curl -fsS -X POST \
            "https://${HOST}/api/${PROJECT_ID}/store/" \
            -H "Content-Type: application/json" \
            -H "X-Sentry-Auth: Sentry sentry_version=7,sentry_key=${PUBLIC_KEY},sentry_client=pages-regenerate/1.0" \
            -d "{\"level\":\"error\",\"logger\":\"github.actions.pages-regenerate\",\"platform\":\"other\",\"message\":\"pages-regenerate failed (SEO/GEO surface stale). Run ${GITHUB_RUN_ID}.\",\"tags\":{\"workflow\":\"pages-regenerate\",\"run_id\":\"${GITHUB_RUN_ID}\"}}" \
            || echo "::warning::Sentry alert POST failed; continuing."
