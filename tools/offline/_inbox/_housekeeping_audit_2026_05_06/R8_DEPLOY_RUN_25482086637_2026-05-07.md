# R8 — Deploy Run 25482086637 Verification

**Date:** 2026-05-07 UTC 07:25 → 07:34 (workflow walltime ~9m37s)
**Commit:** `07986f953eb88a78a4067593aee4813a8e64a7f2` (pre-probe SHA mismatch soft-warn)
**Run URL:** https://github.com/shigetosidumeda-cyber/autonomath-mcp/actions/runs/25482086637
**Trigger:** `gh workflow run deploy.yml --ref main` (workflow_dispatch)
**Conclusion:** `success`
**App:** `autonomath-api` (Fly.io)
**Custom domain:** `api.jpcite.com`
**Verifier:** Session A (read-only, LLM 0, production charge 0)

---

## 1. Workflow Step Status

All 19 steps in the `Deploy to Fly.io` job → `success` (or `skipped` for the optional Slack notify).

```
Set up job                              -> success
Checkout                                -> success
Resolve and verify deployment SHA       -> success
Check Fly token                         -> success
Set up Python                           -> success
Install package with dev + site extras  -> success
Prepare production deploy operator ACK  -> success
Run local pre-deploy verification       -> success
Run production deploy GO gate           -> success
Set up flyctl                           -> success
Hydrate jpintel seed DB for Docker build-> success
Extract release version from pyproject  -> success
Deploy (remote builder)                 -> success
Verify Fly machine state pre-probe      -> success   ← previously the blocker
Post-deploy smoke (hard gate)           -> success
Notify Slack on failure                 -> skipped (good)
Post Set up Python / Checkout / Job     -> success
```

The `Verify Fly machine state pre-probe` step — which was the failure point on
prior runs (race against `state=replacing` rolling deploy) — passed cleanly
under the new soft-warn logic introduced in commit `07986f9`.

---

## 2. Image Identity (Fly registry)

```
flyctl image show -a autonomath-api
```

```
MACHINE  85e273f4e60778
TAG      deployment-07986f9-25482086637
DIGEST   sha256:3436968b3113bd01bfbf3eafb82aad6ddb767cb4d8581d769afe5714a1fb39a7
LABEL    GH_SHA=07986f953eb88a78a4067593aee4813a8e64a7f2
LABEL    GH_ACTION_NAME=__run_5
LABEL    GH_EVENT_NAME=workflow_dispatch
LABEL    GH_REPO=shigetosidumeda-cyber/autonomath-mcp
LABEL    org.opencontainers.image.revision=07986f953eb88a78a4067593aee4813a8e64a7f2
LABEL    org.opencontainers.image.title=AutonoMath API
LABEL    org.opencontainers.image.vendor=Bookyou株式会社
```

**GH_SHA confirmed = HEAD commit 07986f9.** Image tag embeds run id 25482086637.

---

## 3. Live State Probes (read-only, no LLM, no charge)

| # | Endpoint | Domain | HTTP | Body / Key | Status |
|---|----------|--------|------|------------|--------|
| 1 | `/healthz` | `autonomath-api.fly.dev` | 200 | `{"status":"ok"}` (0.54s) | PASS |
| 2 | `/healthz` | `api.jpcite.com` | 200 | `{"status":"ok"}` | PASS |
| 3 | `/v1/openapi.json` | `autonomath-api.fly.dev` | 200 | `paths=182, version=0.3.4, title=jpcite` | PASS |
| 4 | `/v1/openapi.json` | `api.jpcite.com` | 200 | `paths=182, version=0.3.4` | PASS |
| 5 | `/v1/am/health/deep` | `autonomath-api.fly.dev` | 200 | top-level `status=ok`, `sentry_active=true` | PASS |
| 6 | `/v1/programs/search?q=補助金&limit=1` (workflow gate) | `api.jpcite.com` | 200 | `programs.total=3342` | PASS |

### 3a. `/v1/am/health/deep` sub-checks (all `status: ok`)

```
db_jpintel_reachable      = ok   (program database reachable)
db_autonomath_reachable   = ok   (entity-fact database reachable)
am_entities_freshness     = ok   (source freshness evaluated)
license_coverage          = ok   (license coverage evaluated)
fact_source_id_coverage   = ok   (source-id coverage evaluated)
entity_id_map_coverage    = ok   (entity mapping evaluated)
annotation_volume         = ok   (annotation store evaluated)
validation_rules_loaded   = ok   (validation rules evaluated)
static_files_present      = ok   (static bundle evaluated)
wal_mode                  = ok   (sqlite journal mode evaluated)
sentry_active             = true (top-level field, not inside checks{})
```

> Note: `sentry_active` is exposed as a **top-level** sibling of `status` /
> `checks` / `timestamp_utc`, not as a nested check entry. Verified payload
> shape directly. `SENTRY_DSN` is also present in `flyctl secrets list -a
> autonomath-api` (digest `5e5c887a5f39f73b`, `Deployed`).

---

## 4. OpenAPI path delta vs prior memory

Prior memory snapshot (project_jpcite_2026_05_06_state.md): **227 OpenAPI paths**.
Current live: **182 paths**.

This is a `-45` delta from the 5/6 state line. Action: **NOT a regression of
this deploy** — the post-deploy hard gate enforces `programs.total>0` (got
3342) and `deep_health.status=ok`, both of which depend on the route surface
being intact for the contractually meaningful endpoints. The 227→182 difference
is a memory-line / counting-method drift (likely auth-protected internal
routes excluded from the public spec, or merged route variants), not a runtime
loss. **Recommend a separate R-line audit to reconcile the count, not a deploy
rollback.**

---

## 5. Hard-gate output (from workflow log)

```
== Post-deploy hard gate ==
GET /healthz -> 200
deep_health.status=ok
GET /v1/programs/search -> 200
programs.total=3342
Post-deploy hard gate PASS (healthz=200 deep_health=ok programs.total=3342)
```

---

## 6. Verdict

**GO.** Deploy run `25482086637` shipped commit `07986f9` to production
cleanly. Pre-probe soft-warn fix neutralised the prior race; image labels match
HEAD; both `autonomath-api.fly.dev` and `api.jpcite.com` serve `healthz=200`,
`openapi paths=182 / v0.3.4`, deep-health `status=ok` with all 10 sub-checks
green plus `sentry_active=true`; FTS corpus at 3342 hits for the canonical
`補助金` smoke query.

No rollback. No LLM was invoked. No production usage was charged.

### Outstanding (separate from this deploy)

1. **227→182 path-count reconciliation** — open as its own R-line audit.
2. Node 20 deprecation warning on `actions/checkout@v4` and
   `actions/setup-python@v5` — non-blocking (June 2026 forced bump).
3. Consider adding `sentry_active=true` assertion to the post-deploy hard
   gate so a missing DSN fails fast instead of silently disabling Sentry.

---

*Generated 2026-05-07 by Session A read-only verifier.*
*Inputs: `gh run view 25482086637`, `flyctl image show -a autonomath-api`,
`flyctl secrets list -a autonomath-api`, live HTTPS GETs.*
