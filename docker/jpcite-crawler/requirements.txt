# jpcite-crawler runtime dependencies (pinned for AWS Batch credit run 2026-05).
# Keep this list minimal — every wheel adds image size + cold-start time.

# HTTP + parsing
httpx==0.27.2
lxml==5.3.0
beautifulsoup4==4.12.3

# AWS + parquet emit
boto3==1.35.36
botocore==1.35.36
pyarrow==17.0.0

# SHA-256 helper (hashlib covers basic; cryptography enables future Ed25519
# signing on receipts per dim O / explainable_fact contract).
cryptography==43.0.1
