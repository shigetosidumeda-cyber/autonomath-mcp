# jpcite — vendor-neutral agent context shim

> **Source of truth: `AGENTS.md`** at the repo root. Read it first.
> This shim exists so harnesses that look for `.agent.md` first still find the canonical pointer.
> Last canonical edit: 2026-05-17 (Harness H3 — Agent Entry SOT shim).

## See `AGENTS.md`

All project identity, hard constraints, architecture, key commands, quality gates, and "what NOT to do" rules live in `AGENTS.md`. Do not duplicate or override them here.

For live counts (MCP tools / REST routes / OpenAPI paths / program counts):

- `scripts/distribution_manifest.yml` — canonical published counts.
- `len(await mcp.list_tools())` — runtime MCP tool count.
- `python scripts/probe_runtime_distribution.py` — runtime + manifest one-pass probe.

## Companion shims

- `CLAUDE.md` — Claude-specific operating notes (still defers to AGENTS.md).
- `.cursorrules` — Cursor IDE auto-injection shim.
- `.windsurfrules` — Windsurf IDE auto-injection shim.
- `.mcp.json` — MCP client root config (stdio entry).
