この1週間、{{usage_count}} 回のクエリ。

キー末尾 {{key_last4}} ({{tier}} プラン) で、あなたは {{usage_count}} 回のリクエストを送ってくださいました。ありがとうございます。

今日は、まだ試していない方向けに「AutonoMath の2つの効く使い方」をご紹介します。

---

1) 排他ルール (exclusions.check) で二重申請リスクを潰す

日本の補助金・交付金には「この A と B は同時に受給不可」というルールが無数にあります。AutonoMath には公式ソースから抽出した exclusion_rules を載せていて、2つの unified_id を渡すだけで判定できます。

curl -H "X-API-Key: YOUR_KEY" \
  "https://api.autonomath.ai/v1/exclusions/check?a=UNI-xxxx&b=UNI-yyyy"
-> { "blocked": true, "kind": "absolute", "source": "..." }

クライアントに「併用不可リスクの自動チェック」を出せるのは、この API の一番の強みです。

2) source_mentions で回答を監査可能にする

すべての program レスポンスには source_mentions_json が付いています。
「この金額はどの公式文書の何ページに載っていたのか」を辿れるので、
LLM の回答を監査にかけるユーザーには必須です。

curl -H "X-API-Key: YOUR_KEY" \
  https://api.autonomath.ai/v1/programs/UNI-xxxx \
  | jq '.source_mentions_json'

---

他の使い方: https://autonomath.ai/examples/

感想・改善点は、このメールに返信で結構です。

--
AutonoMath — 日本の制度情報 API + MCP
お問い合わせ: info@bookyou.net
運営: Bookyou 株式会社 (T8010001213708) / 東京都文京区小日向2-22-1
配信停止: {{{pm:unsubscribe}}}
