玄人向けの 3 つの省力化 Tips

キー末尾 {{key_last4}} ({{tier}} プラン) を発行してから 2 週間、{{usage_count}} 回ご利用いただきました。「ループ叩きで雑コーディングしている」段階から「予算と運用が整った」段階に移れる、よく効く 3 つの設定だけお送りします。

---

1) MCP 経由でループを LLM に押し込む

   curl で 1 件ずつ叩いている場合、Claude Desktop / Cursor / Cline から MCP 経由で呼ぶと、
   自然言語側でクエリ生成と結果の正規化がそのまま噛みます。下の設定を 1 回入れれば、
   以降は「東京都の中小企業向けで 1000 万円以上の補助金を10件、それぞれ排他ルールも付けて」と
   書くだけです。

   {
     "mcpServers": {
       "autonomath": {
         "command": "uvx",
         "args": ["autonomath-mcp"],
         "env": { "JPCITE_API_KEY": "jc_xxx" }
       }
     }
   }

   クライアント別セットアップ詳細: https://jpcite.com/integrations/

2) /programs/batch で N 倍の節約

   取引先の DD / 申請候補のスクリーニングなど、N 件まとめて取りに行くなら
   1 リクエストにまとめるのが効きます。N 個の get_program を 1 回の POST にできて、
   課金も 1 回分です。

   curl -X POST -H "X-API-Key: jc_xxx" -H "Content-Type: application/json" \
     "https://api.jpcite.com/v1/programs/batch" \
     -d '{"unified_ids":["UNI-14e57fbf79","UNI-40bc849d45","UNI-08d8284aae"]}'

   取引先の法人番号 N 件は /v1/am/dd_batch にまとめられます。50 件まで
   1 リクエスト・¥3.30 で、補助金採択 / 行政処分 / 適格請求書発行登録を
   一括確認できます。

3) monthly_cap_yen で予算上限を縛る

   従量課金の不安は「気づいたら ¥10 万」に集中します。鍵単位の月次 ¥ ハード上限を
   1 行で設定できます (JST 月初 00:00 リセット)。例えば月 ¥3,000 まで:

   curl -X POST -H "X-API-Key: jc_xxx" -H "Content-Type: application/json" \
     "https://api.jpcite.com/v1/me/cap" \
     -d '{"monthly_cap_yen": 3000}'

   上限到達後はそのキーが 402 Payment Required を返します。本番アプリでも
   安心して使えるようになります。解除は {"monthly_cap_yen": null} です。

---

他の運用 Tips:
- Getting Started: https://jpcite.com/docs/getting-started/
- examples 集: https://jpcite.com/examples/

ご質問・改善点・「こうしたい」は、このメールに返信ください。人間が読みます。

---

税理士法 §52 / 弁護士法 §72 該当の業務はおこないません:

jpcite は日本の制度情報の検索インデックス API です。税務助言・法律相談・申請代行・社会保険労務士業務はおこないません。出力結果は士業による確認の代替にはなりません。

--
jpcite — 日本の制度情報 API + MCP
このメールは jpcite の活性化シーケンス (D+14) としてお送りしています。
お問い合わせ: info@bookyou.net
運営: Bookyou 株式会社 (T8010001213708) / 東京都文京区小日向2-22-1
配信停止: {{{pm:unsubscribe}}}
