---
name: jpcite_com_dns_runbook
alias_of: autonomath_com_dns_runbook
brand_layer: jpcite
legacy_brand: autonomath
wave: 46.D
generated_at: 2026-05-12
status: alias (banner-only; canonical content unchanged)
---

# jpcite.com DNS runbook (alias of autonomath_com_dns_runbook)

> **Alias notice (Wave 46.D, 2026-05-12)**
>
> This document is the **jpcite-branded alias** of the canonical operator
> DNS runbook previously published under the AutonoMath brand. As part of
> Wave 46 brand consolidation (zeimu-kaikei.ai / AutonoMath → **jpcite**),
> this file points readers from the new brand surface to the unchanged
> canonical source. **No content has been migrated or rewritten** — see
> the source document for the authoritative, current procedure.
>
> The original file is preserved at its original path under the legacy
> brand name to keep historical links, archive references and prior PR
> descriptions working (per `feedback_destruction_free_organization` —
> rm/mv 禁止, banner+index で整理).
>
> - **Canonical source (unchanged)**:
>   [`autonomath_com_dns_runbook.md`](./autonomath_com_dns_runbook.md)
> - **Brand history note**: the legacy brand marker is intentionally
>   understated (per `feedback_legacy_brand_marker` — do not surface
>   the old brand prominently; this is an SEO-citation bridge only).
> - **Redirect index**:
>   [`REDIRECT_MAP_w46_canonical.md`](./REDIRECT_MAP_w46_canonical.md)

## How to read this alias

1. Open the canonical source linked above — that is the single source of truth.
2. This alias exists so that grep / sitemap / AI-agent retrieval that
   look up `jpcite_com_dns_*` resolve to a real markdown asset and find
   their way to the canonical content in one hop.
3. **Do not edit this file** to change DNS procedure — edit the canonical
   source instead. Aliases are diff-frozen.

## Scope summary (copied from canonical for grep-discoverability only)

The canonical document covers, in this order:

- Current DNS diagnosis (`jpcite.com` Cloudflare, `autonomath.com` legacy,
  `.ai` parity, NS / A / CNAME / TXT verification by `dig`).
- 整合 plan: which subdomains should resolve to which origins
  (`api.jpcite.com`, `docs.jpcite.com`, root, www, redirects).
- Cloudflare records to add / verify, with TTL and proxied-flag guidance.
- Verification steps (`dig`, `curl -I`, TLS handshake, CSP / HSTS check).
- Rollback (Cloudflare zone snapshot, fall back to prior NS).

**Do not** treat the bullets above as authoritative — they exist only so
that an agent or operator searching by phrase finds the alias and
follows the link to the real runbook.

## Why an alias instead of a rename

- `rm` / `mv` is forbidden under `feedback_destruction_free_organization`.
- The canonical filename is load-bearing for prior PR descriptions,
  release notes, archived links and external mirrors.
- Brand consolidation is served fully by adding a *new* jpcite-named
  alias surface — the legacy surface incurs zero rewrite cost and zero
  linkrot risk.

## Verification

- Alias generated by **Wave 46.D** (`tick5#5`) on 2026-05-12.
- Canonical source byte-content is unchanged (verified by
  `tests/test_w47d_docs_canonical_alias.py`).
- See `docs/_internal/REDIRECT_MAP_w46_canonical.md` for the full
  Wave 46.D alias table.
