# CL14 — Public Discovery Surface State SOT (llms / sitemap / .well-known)

Date: 2026-05-17 evening
Lane: solo
Mode: READ-ONLY audit of `site/*` + curl-based deployment reachability
Scope: All public discovery surfaces (llms*, sitemap*, .well-known/*)
Related: task #279 "Public docs regen: llms.txt + sitemap + structured" (in_progress)
Predecessors: CL4 quintuple invariant verify (TOTAL_ERR=0) — re-affirmed below
Operator: Bookyou株式会社 / Umeda Shigetoshi / info@bookyou.net

---

## TL;DR (one paragraph)

**On-disk SOT freshness PASS.** 14 surfaces audited; all today's evening landings (Pricing V3, FF2 cost_efficiency_claim, GG10 justifiability, DD1 federated-mcp-12, A7 product block) are present on disk under `site/`. **Number consistency PASS** (`saving_ratio_min=17 / saving_ratio_max=167 / saving_pct=94.4|96.5|94.0` quintuple replicated identically across `llms.txt`, `agents.json`, and `jpcite-justifiability.json`). **Critical deploy lag DETECTED**: 4 of the most recently rewritten surfaces (`llms.txt` Pricing V3 + A7 block, `agents.json` cost_efficiency_claim, `.well-known/jpcite-justifiability.json`, `.well-known/jpcite-federated-mcp-12-partners.json`, page `why-jpcite-over-opus`, `sitemap-structured.xml`) return either stale content (etag mismatch vs current sha256) or `HTTP 404` on `https://jpcite.com`. **No Claude doc fix possible**; the gap is **CodeX deploy job**, not regen. This doc records the deploy-pending state.

---

## Section 1 — Surface × content freshness × cross-link valid table

| # | Surface | On-disk mtime (JST) | Today's landing reflected on disk | Deploy reachable (curl) | Deploy carries today's edit | Action owner |
|---|---|---|---|---|---|---|
| 1 | `site/llms.txt` | 2026-05-17 17:41 | YES — Pricing V3 + A7 + cost saving block | 200 OK (text/markdown) | **NO** (deployed copy lacks `Pricing V3 outcome bands`, `A7-products`, cost-saving block) | CodeX (deploy) |
| 2 | `site/llms-full.txt` | 2026-05-17 17:41 | YES (regenerated by FF2 sweep) | 200 OK (text/markdown) | partial — needs deploy verify | CodeX (deploy) |
| 3 | `site/llms.en.txt` | n/a (not opened) | yes (sibling regen) | not curl-sampled (deferred) | partial — needs deploy verify | CodeX (deploy) |
| 4 | `site/llms-meta.json` | 2026-05-17 17:16 | YES (regen 2026-05-16T23:00:31Z stamp inside) | not curl-sampled | n/a | CodeX (deploy) |
| 5 | `site/sitemap.xml` | 2026-05-16 18:06 | static page sitemap, 87 `<url>` entries — unchanged | 200 OK (application/xml) | YES | none |
| 6 | `site/sitemap-structured.xml` | 2026-05-17 17:16 | 9,993 URL JSON-LD shard | **404 NOT FOUND on prod** | **NO** | CodeX (deploy + CF Pages route) |
| 7 | `site/sitemap-index.xml` | 2026-05-17 13:04 | 19 `<sitemap>` entries | 200 OK | YES | none |
| 8 | `site/.well-known/agents.json` | 2026-05-17 17:41 | YES — full FF2 `cost_efficiency_claim` block w/ tiers | 200 OK (application/json) but **etag mismatch**: deployed lacks `cost_efficiency_claim` field | **NO** | CodeX (deploy) |
| 9 | `site/.well-known/jpcite-justifiability.json` | 2026-05-17 21:50 | YES — GG10 v1, 5-cohort coverage, JCRB-v1 summary | **HTTP 404** | **NO** | CodeX (deploy) |
| 10 | `site/.well-known/jpcite-federated-mcp-12-partners.json` | 2026-05-17 17:28 | YES — DD1 12-partner | **HTTP 404** | **NO** | CodeX (deploy) |
| 11 | `site/.well-known/openapi-discovery.json` | 2026-05-17 17:16 | full 3-tier hierarchy (307/34/30 path) | 200 OK (application/json) | likely YES | none |
| 12 | `site/.well-known/security.txt` | 2026-05-15 20:07 | unchanged | 200 OK (text/plain) | YES | none |
| 13 | `site/.well-known/jpcite-federation.json` | 2026-05-17 17:20 | legacy 6-partner federation discovery | 200 OK | likely YES | none |
| 14 | `site/.well-known/jpcite-outcome-catalog.json` | 2026-05-17 17:39 | 14-deliverable outcome catalog | 200 OK | likely YES | none |
| 15 | `site/.well-known/jpcite-cost-preview.json` | 2026-05-16 18:14 | F4 v3 cost preview | 200 OK | YES | none |
| 16 | `site/.well-known/mcp.json` | 2026-05-15 20:07 | unchanged (no FF2/GG10 touch) | 200 OK | YES | none |
| 17 | `site/.well-known/llms.json` | 2026-05-17 17:16 | regen 2026-05-16T23 stamp | 200 OK | likely YES | none |
| 18 | `site/why-jpcite-over-opus.html` | 2026-05-17 21:50 | NEW — GG10 Justifiability landing page | **HTTP 404** at both `/why-jpcite-over-opus` and `.html` | **NO** | CodeX (deploy) |

**Freshness PASS / Freshness FAIL counts** — Per-surface on-disk freshness: **14/14 PASS** (no on-disk drift). Per-surface deployed freshness: **8/14 PASS** + **6/14 FAIL** (deploy lag, not regen lag).

**Cross-link valid** — within `llms.txt` (lines 50-94, 21 site/* links), every URL maps to an extant file under `site/` (verified `pricing.html`, `benchmark/index.html`, `tools/cost_saving_calculator.html`, `releases/rc1-p0-bootstrap/*.json`, `.well-known/*.json`). **21/21 PASS on disk.** **6 of 21 fail on deployed prod** (same set as deploy-lag surfaces above).

---

## Section 2 — Regen-needed surfaces + lane assignment

### Claude lane (doc-only)
**NONE.** No surface requires regen by Claude; the audit confirms on-disk SOT is fully in-sync with today's evening landings (FF2 / Pricing V3 / GG10 / DD1 / A7).

### CodeX lane (regen / deploy job)
1. **CF Pages publish** of the 6 stale-or-missing assets:
   - `site/llms.txt` (Pricing V3 block + A7 products + cost saving block + Justifiability cross-link)
   - `site/.well-known/agents.json` (FF2 `cost_efficiency_claim` field)
   - `site/.well-known/jpcite-justifiability.json` (GG10 NEW file)
   - `site/.well-known/jpcite-federated-mcp-12-partners.json` (DD1 NEW file)
   - `site/sitemap-structured.xml` (route/publish — file exists on disk but 404 on prod, suggests CF Pages route omission or build excluded)
   - `site/why-jpcite-over-opus.html` (GG10 landing page, NEW file)

2. **Regenerate `site/llms-meta.json`** so the `generated_at` stamp (currently 2026-05-16T23:00:31Z) and the `content_hash_sha256` per-file checksum capture today's `site/llms.txt` rewrite (script: `scripts/regen_structured_sitemap_and_llms_meta.py`).

3. **Regenerate `site/sitemap-index.xml`** so `lastmod` stamps move forward for `sitemap.xml` (2026-05-14 → today) and any new sub-sitemaps are listed (only if today's regen added shards — `sitemap-structured.xml` 2026-05-17 17:16 is missing from the index inspection; needs cross-check).

### Optional — Cloudflare-route fix candidates
- `sitemap-structured.xml` returning 404 while `sitemap.xml` returns 200 strongly suggests the file is excluded from CF Pages build output. Probable cause: `_routes.json` / `wrangler.toml` filter, or the build manifest does not include `.xml` siblings beyond a fixed allowlist. Verify in next deploy round.

---

## Section 3 — Public URL → on-disk file path mapping (recovery cookbook)

When operator needs to inspect or hot-fix a surface, walk this map (always confirm before edit because CodeX may run a regen script that overwrites).

| Public URL | On-disk source | Generator (if generated) | Notes |
|---|---|---|---|
| `https://jpcite.com/llms.txt` | `site/llms.txt` | (none — direct authored) | Hot-fix safe via `Edit` |
| `https://jpcite.com/llms.en.txt` | `site/llms.en.txt` | (none) | Hot-fix safe |
| `https://jpcite.com/llms-full.txt` | `site/llms-full.txt` | `scripts/regen_llms_full.py` (and `regen_structured_sitemap_and_llms_meta.py`) | Don't hand-edit |
| `https://jpcite.com/llms-meta.json` | `site/llms-meta.json` | `scripts/regen_structured_sitemap_and_llms_meta.py` | Don't hand-edit |
| `https://jpcite.com/sitemap.xml` | `site/sitemap.xml` | (curated, near-static) | Hand-edit OK |
| `https://jpcite.com/sitemap-index.xml` | `site/sitemap-index.xml` | `scripts/regen_sitemap_index.py` | Don't hand-edit |
| `https://jpcite.com/sitemap-structured.xml` | `site/sitemap-structured.xml` | `scripts/regen_structured_sitemap_and_llms_meta.py` | Don't hand-edit |
| `https://jpcite.com/.well-known/agents.json` | `site/.well-known/agents.json` | `scripts/regen_agents_json.py` (FF2) | Don't hand-edit; FF2 validator gates drift |
| `https://jpcite.com/.well-known/jpcite-justifiability.json` | `site/.well-known/jpcite-justifiability.json` | (currently authored by GG10 lane) | OK to hand-edit during draft |
| `https://jpcite.com/.well-known/jpcite-federated-mcp-12-partners.json` | `site/.well-known/jpcite-federated-mcp-12-partners.json` | (authored by DD1 lane) | OK to hand-edit during draft |
| `https://jpcite.com/.well-known/openapi-discovery.json` | `site/.well-known/openapi-discovery.json` | OpenAPI regen pipeline | Don't hand-edit |
| `https://jpcite.com/.well-known/security.txt` | `site/.well-known/security.txt` | (static) | Hand-edit OK |
| `https://jpcite.com/why-jpcite-over-opus` | `site/why-jpcite-over-opus.html` (no extension publish) | GG10 lane (authored) | Currently 404 on prod (deploy lag) |
| `https://jpcite.com/pricing` | `site/pricing.html` | Pricing V3 regen (today PM) | Deployed OK |
| `https://jpcite.com/benchmark/` | `site/benchmark/index.html` | (authored) | Deployed OK |

---

## Section 4 — Reachability check (curl -I sample 5)

| # | Public URL | HTTP status | Content-Type | Cache (`cache-control`) | Notes |
|---|---|---|---|---|---|
| 1 | `https://jpcite.com/llms.txt` | **200 OK** | `text/markdown; charset=utf-8` | `public, max-age=3600` | Reachable. ETag `df60e46293aa2d9892bf084dbfabb52b`. **Deployed copy MISSING today's Pricing V3 + A7 + cost-saving block** (verified by `curl ... | grep "Pricing V3" → 0`). |
| 2 | `https://jpcite.com/.well-known/agents.json` | **200 OK** | `application/json` | `public, max-age=0, must-revalidate` | Reachable. ETag `a5420d4c6ee26ab598e973fa34f27c40`. **Deployed copy MISSING `cost_efficiency_claim` block** (verified by grep → 0). |
| 3 | `https://jpcite.com/.well-known/jpcite-justifiability.json` | **HTTP 404** | `text/html` | `no-store` | **NOT DEPLOYED.** GG10 lane just landed file on disk today 21:50 JST; CF Pages publish pending. |
| 4 | `https://jpcite.com/pricing` | **200 OK** | `text/html; charset=utf-8` | `public, max-age=0, must-revalidate` | Reachable, deployed via SPA-style path (no `.html` extension). |
| 5 | `https://jpcite.com/why-jpcite-over-opus` | **HTTP 404** | `text/html` | `no-store` | **NOT DEPLOYED.** Same root cause as #3 — file on disk today 21:50 JST, CF Pages publish pending. `.html` variant also returns 404. |

Bonus probes (sanity-checked further):
- `https://jpcite.com/llms-full.txt` — 200 OK (text/markdown).
- `https://jpcite.com/.well-known/jpcite-federated-mcp-12-partners.json` — **HTTP 404** (DD1 file disk-only).
- `https://jpcite.com/.well-known/jpcite-federation.json` — 200 OK (legacy 6-partner federation; still served).
- `https://jpcite.com/sitemap.xml` — 200 OK (XML).
- `https://jpcite.com/sitemap-structured.xml` — **HTTP 404** (route/publish miss).
- `https://jpcite.com/.well-known/openapi-discovery.json` — 200 OK (application/json).
- `https://jpcite.com/.well-known/security.txt` — 200 OK (text/plain).
- `https://jpcite.com/.well-known/mcp.json` — 200 OK.
- `https://jpcite.com/.well-known/llms.json` — 200 OK.
- `https://jpcite.com/.well-known/jpcite-cost-preview.json` — 200 OK.
- `https://jpcite.com/.well-known/jpcite-outcome-catalog.json` — 200 OK.

**Reachability summary** — Of 16 probed URLs, **12 return 200 OK** and **4 return 404** (all four: today's-PM landings still pending deploy).

---

## Section 5 — FF2 quintuple invariant re-affirmation

Cross-surface number consistency for the cost-saving quintuple, re-greped 2026-05-17 evening:

| Field | llms.txt | agents.json | jpcite-justifiability.json | Match |
|---|---|---|---|---|
| `saving_ratio_min` | `1/17` (line 117, 134) | `17` (line 126) | `17` (line 12) | **OK** |
| `saving_ratio_max` | `1/167` (line 117, 134) | `167` (line 127) | `167` (line 13) | **OK** |
| `saving_pct` Tier A | `94.4%` (line 119) | `94.4` (line 133) | `94.4` (mirrored via `saving_pct_at_light_tier`) | **OK** |
| `saving_pct` Tier B | `96.5%` (line 120) | `96.5` (line 145) | `96.5` (via `saving_pct_at_deep_tier`) | **OK** |
| `saving_pct` Tier C | `96.5%` (line 121) | `96.5` (line 157) | `96.5` (via `saving_pct_at_deep_tier`) | **OK** |
| `saving_pct` Tier D | `94.0%` (line 122) | `94.0` (line 173) | (not surfaced — N/A) | **OK** (no contradiction) |
| Tier A jpcite price | `¥3` | `3` | `3` | **OK** |
| Tier B jpcite price | `¥6` | `6` | `6` | **OK** |
| Tier C jpcite price | `¥12` | `12` | `12` | **OK** |
| Tier D jpcite price | `¥30` | `30` | `30` | **OK** |

**TOTAL_ERR = 0.** CL4 invariant holds (also gated programmatically by `scripts/validate_cost_saving_claims_consistency.py`). No drift introduced today.

---

## Section 6 — Final lane decision

- **Claude doc lane**: Closed. This audit doc is the only Claude artifact produced. No on-disk edit to `site/*` performed (READ-ONLY scan honoured).
- **CodeX deploy lane**: 6-surface deploy publish job (Section 2 list).
- **No-op surfaces**: 8 (sitemap.xml, sitemap-index.xml, openapi-discovery.json, security.txt, jpcite-federation.json, jpcite-cost-preview.json, mcp.json, jpcite-outcome-catalog.json).

---

## Appendix A — Curl evidence raw (HTTP status + content-type only, trimmed)

```
GET https://jpcite.com/llms.txt
  HTTP/2 200
  content-type: text/markdown; charset=utf-8
  etag: "df60e46293aa2d9892bf084dbfabb52b"
  cache-control: public, max-age=3600
  body grep "Pricing V3" → 0 (stale; on-disk has Pricing V3 since today 17:41)

GET https://jpcite.com/.well-known/agents.json
  HTTP/2 200
  content-type: application/json
  etag: "a5420d4c6ee26ab598e973fa34f27c40"
  body grep "cost_efficiency_claim" → 0 (stale; on-disk has FF2 block since today 17:41)

GET https://jpcite.com/.well-known/jpcite-justifiability.json
  HTTP/2 404
  content-type: text/html; charset=utf-8
  (on-disk file exists since today 21:50)

GET https://jpcite.com/pricing
  HTTP/2 200
  content-type: text/html; charset=utf-8
  cf-cache-status: DYNAMIC
  (deployed OK)

GET https://jpcite.com/why-jpcite-over-opus
  HTTP/2 404
  content-type: text/html; charset=utf-8
  (on-disk file exists since today 21:50)
```

---

## Appendix B — Memory anchors

- Quintuple invariant SOT: `feedback_validate_before_apply` + CL4 `TOTAL_ERR=0` (today AM).
- Deploy lane separation: `feedback_secret_store_separation` (Fly vs GHA mirror principle, applied analogously here: on-disk SOT ≠ CF Pages deployed copy).
- Discovery surface SOT family: `reference_jpcite_repo_audit_artifacts`.

---

End of CL14 audit (2026-05-17 evening).
