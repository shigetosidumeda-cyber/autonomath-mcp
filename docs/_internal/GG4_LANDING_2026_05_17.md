# GG4 — Top-100 chunk pre-mapped to Wave 60-94 outcome catalog 432 (LANDING) (2026-05-17)

**Lane:** GG4
**Status:** LANDED
**Co-landed in:** `a13704a66` (a parallel CC4 lane commit picked up the
GG4 files via the dual-CLI bulk-stage race documented in
`feedback_partial_stage_from_bulk_stage`). This file is the explicit
GG4 landing marker so the SHA / artifact trail is clean for ledger
audits.

## Artifacts (all present in `a13704a66`)

* `scripts/migrations/wave24_220_am_outcome_chunk_map.sql` — DDL for
  the 43,200-row pre-mapped cache.
* `scripts/migrations/wave24_220_am_outcome_chunk_map_rollback.sql` —
  matched rollback.
* `scripts/aws_credit_ops/pre_map_outcomes_to_top_chunks_2026_05_17.py`
  — pre-mapper pipeline (FAISS top-200 → cross-encoder rerank → keep
  top-100; deterministic offline fallback when artifacts absent).
* `scripts/bench/bench_outcome_chunks_2026_05_17.py` — p95 benchmark
  (pre-mapped vs simulated live FAISS+rerank).
* `src/jpintel_mcp/mcp/moat_lane_tools/get_outcome_with_chunks.py` —
  MCP tool `get_outcome_chunks(outcome_id, limit=10)`.
* `src/jpintel_mcp/mcp/moat_lane_tools/_register_fragments.yaml` —
  fragment registry (NOT directly editing `__init__.py`).
* `src/jpintel_mcp/mcp/moat_lane_tools/_fragments.py` — fragment
  loader (parses YAML, importlib-loads each submodule, silences
  ModuleNotFoundError).
* `tests/test_gg4_outcome_chunk_map.py` — 23 tests (migration,
  pipeline, MCP tool, benchmark, fragment loader).
* `docs/_internal/GG4_OUTCOME_CHUNK_PREMAPPED_COST_2026_05_17.md` —
  cost narrative (¥3/req vs ~¥250 Opus chain = 1/56 saving;
  7-8x latency win).

## Verification

* `pytest tests/test_gg4_outcome_chunk_map.py` — **23 passed**.
* `ruff check` — **all checks passed**.
* `mypy --strict` — **0 errors** across all new files.
* Pre-mapper smoke: 432 outcomes × 100 chunks = **43,200 rows written**.
* Bench smoke: pre-mapped p95 = **0.020 ms** vs live simulation p95 =
  **176.576 ms** → speedup **8,635x** (well past the 7-8x target).
