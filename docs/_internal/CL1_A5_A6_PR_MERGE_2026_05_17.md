# CL1 A5+A6+P4+P5 — PR Merge Log (2026-05-17)

## Outcome: PR #245 OPEN as DRAFT (blocked, operator decision pending)

Worktree branch `worktree-agent-ac0ac5fdd0bcff29c` (head `fa3c80a47`) was
pushed and PR #245 was opened against `main`. Merge was **not** executed
because mechanical rebase would clobber 42 commits of in-flight CodeX work
on `main`. Per the operator constraint "既存 main work を一切壊さない",
the PR is converted to **draft** and a detailed blocking comment is posted.

## PR Identity

| field | value |
| --- | --- |
| PR number | **245** |
| URL | https://github.com/shigetosidumeda-cyber/autonomath-mcp/pull/245 |
| Base | `main` |
| Head | `worktree-agent-ac0ac5fdd0bcff29c` |
| Head SHA | `fa3c80a47` |
| State | OPEN |
| isDraft | **true** |
| Mergeable | CONFLICTING (DIRTY) |
| Auto-merge | NOT AVAILABLE (no branch protection rules) |

## Divergence Analysis

```
git merge-base origin/main origin/worktree-agent-ac0ac5fdd0bcff29c
  = 8076edaf9 (harness-H6 part 3)

worktree ahead of merge-base: 1 commit  (fa3c80a47 A5+A6+P4+P5)
main     ahead of merge-base: 42 commits

git diff origin/main..worktree --stat
  212 files changed, +5,311 / -61,619
```

A mechanical rebase or squash-merge would destroy substantial in-flight
main work, including:

- **Pricing V3** — `src/jpintel_mcp/billing/pricing_v3.py` plus A1/A2/A3
  product re-prices. **Directly supersedes A6 pricing_v2** shipped in this PR.
- GG1 HE-5/HE-6 cohort heavy endpoints (10 tools, 432 × 5 cohort)
- GG7 outcome cohort variants (2,160 rows) + MCP tool
- GG10 Justifiability landing + `/why` redirect
- M1 PDF→KG LIVE (108,077 entity facts + 99,929 relations)
- M3/M7 SageMaker code-channel + PyKEEN dash-hyperparameters
- FF1 jpcite cost ROI SOT + P5 benchmark ground-truth bundle
- FF2 cost-saving narrative + test suite
- AA1-G1 NTA QA + 裁決 + 地方税 ETL substrate
- AA3+AA4 G7 FDI + G8 時系列 LIVE (body_en 1→13,542, treaty 33→54)
- CC4 PDF watch + Textract + spaCy KG pipeline
- DD1 federated MCP 12-partner
- DD2 municipality subsidy OCR
- EE1 burn rate monitor 15-lane audit
- Multiple harness-H6 SDK route drift fixes

## Direct Contract Conflict

| topic | worktree (this PR) | main (already landed) |
| --- | --- | --- |
| Pricing module | `pricing_v2.py` (PricingTier enum, 4 tier) | `pricing_v3.py` (A=1/B=2/C=4/D=10 billable_units, JPY 3/6/12/30) |
| A1/A2/A3 product pricing | (untouched) | re-priced to V3 |
| pricing test file | `test_billing_pricing_v2.py` (30 PASS) | `test_billing_pricing_v3.py` (256 LOC) |

The A6 stream in this PR is **already obsoleted** by Pricing V3 on main.

## Quality (worktree-side, pre-merge)

- pytest: **49/49 PASS** on the worktree branch
  - `tests/test_product_a5_kaisha_setsuritsu.py` — 19 PASS
  - `tests/test_billing_pricing_v2.py` — 30 PASS (note: superseded surface)
  - `tests/test_no_llm_in_production.py` — 10 PASS
- mypy `--strict` on `src/jpintel_mcp/mcp/products/` — 0 error
- ruff `src/` `scripts/` — 0 error
- `scripts/safe_commit.sh` full strict hooks PASS at commit time

## Actions Taken

1. Fetched `origin/main` and `origin/worktree-agent-ac0ac5fdd0bcff29c`
2. Computed merge-base + diff stat
3. Listed open PRs (none against worktree branch before this run)
4. **Created PR #245** via `gh pr create` (no `--force`, no `--no-verify`)
5. Attempted `gh pr merge 245 --squash --auto` → rejected
   (`Pull request Protected branch rules not configured for this branch`)
6. Re-inspected divergence — discovered 42-commit main drift
7. Determined mechanical rebase would clobber Pricing V3 + 41 other commits
8. Posted blocking comment on PR #245 enumerating affected main work
9. Converted PR #245 to **draft** to prevent accidental merge
10. Wrote this doc on a `doc/cl1-a5-a6-merge-2026-05-17` branch off
    `origin/main` for separate doc-only PR

## Recommended Path Forward (operator decision)

**Option A — Cherry-pick selective (recommended)**
1. Branch off `origin/main`
2. Cherry-pick only **A5** (kaisha-setsuritsu), **P4** (freshness),
   **P5** (quality benchmark) from `fa3c80a47`
3. **Skip A6** pricing_v2 (already superseded by Pricing V3)
4. Re-test (expect 19 A5 + 10 NO_LLM = 29 PASS subset)
5. Open fresh PR

**Option B — Re-baseline worktree**
1. On worktree: `git fetch origin && git rebase origin/main`
2. Manually resolve 200+ file conflicts (significant time)
3. Discard `pricing_v2.py` in favor of `pricing_v3.py`
4. Force-push with `--force-with-lease` (worktree branch only — main untouched)
5. Re-test full suite
6. Re-open PR for merge

**Option C — Discard worktree branch**
1. Close PR #245 unmerged
2. Re-implement A5/P4/P5 fresh on top of current `origin/main`

## Constraints Honored

- ✅ NO direct commit to `main` (all activity via `gh pr` and worktree branches)
- ✅ NO `git push --force` / `--force-with-lease` against `main`
- ✅ NO `--no-verify` anywhere
- ✅ Co-Authored-By trailer present on doc commit
- ✅ `scripts/safe_commit.sh` used for this doc commit
- ✅ CodeX collision avoidance: main untouched; PR draft state prevents
  accidental merge

## Cross-References

- `feedback_no_user_operation_assumption.md` — verified divergence with
  `git diff --stat` (212 files, -61,619 lines) before flagging operator decision
- `feedback_dual_cli_lane_atomic.md` — main is CodeX lane, no claim taken
- `feedback_completion_gate_minimal.md` — minimal blocker: mechanical merge
  would destroy 42 main commits; this is the **only** completion gate
- `feedback_overwrite_stale_state.md` — A6 pricing_v2 is stale, V3 is SOT
- `feedback_serial_lane_for_contended_refactor.md` — 212-file divergence is
  textbook contended-refactor; serial cherry-pick is the safe path
