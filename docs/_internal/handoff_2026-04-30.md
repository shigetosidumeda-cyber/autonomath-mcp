# Handoff 2026-04-30 (JST 21:30)

このドキュメントは 2026-04-30 のセッション (jpcite Claude CLI 単独 + OTHER CLI 並列) の引き継ぎ。次セッションがどこから再開するかを最短で把握できる形にする。

## 1 行でいうと

> 今日 1 日で **MCP Registry 公開・PyPI 0.3.2 公開・anon 50/月 → 3/日 移行・Evidence Layer ピボット完了・perf 33GB 解放** まで届いた。残りは ① migration 120 の自動 deploy 確認、② OTHER CLI が出した 4 つの研究 deliverable のレビュー、③ outreach メール 5 通。

---

## 2. 本日の確定事実

### 2.1 production 状態

| 項目 | 状態 |
|---|---|
| Fly machine v40 | started, 1/1 passing, 9.4 GB DB attached |
| `/v1/usage` (anon) | tier=anonymous, **limit=3**, reset=JST 翌日 00:00 |
| MCP Registry | `io.github.shigetosidumeda-cyber/autonomath-mcp` v0.3.2 公開済 |
| PyPI | `autonomath-mcp` 0.3.2 公開済 (mcp-name validation 通過) |
| GitHub Actions | `mcp-registry-publish.yml` (OIDC、no PAT 必要) 動作確認済 |
| MCP tool count | 89 → **93** (citations / evidence_packet / funding_stack / source_manifest を `__init__.py` に追記) |
| Pages | 7842182b 〜 79f590d1 で複数 deploy。dark mode 構造的 guard + Bookyou 見直し反映済 |
| Total routes | **203** (4 routers mount で +7) |

### 2.2 完了マイルストーン

- ✅ migration 116 hotfix (`usage_events.quantity` 列追加 → Fly deploy 復旧)
- ✅ anon limit 50/月 → 3/日 (config + helpers + 30+ サイトページコピー)
- ✅ value_max v2 plan を `_archive/2026-04/` に移動、LR plan を唯一の正本へ
- ✅ Evidence Packet composer / token_compression / citation_verifier / funding_stack_checker / audit_seal wire-up / corpus_snapshot endpoint
- ✅ adoption→program join (migration 114)、weekly digest (113)、source_manifest view (115)、usage_events index (116)、source_url_status (118)、audit_seal seal_id (119)
- ✅ "Token Cost Shield" → Evidence Pre-fetch Layer に rename + 危険コピー (一次資料 100% / 全件 / 必ず) 削除
- ✅ Hero "運営: Bookyou株式会社" chip 削除 (法的開示は footer/tos/tokushoho に集中)
- ✅ DARK mode 構造的 guard (`@media(prefers-color-scheme:dark)` + 4 utility classes)
- ✅ DIRECTORY.md sync, docs/_internal/ archive, performance audit doc
- ✅ scipy lazy load (API boot 5.45s → 3.52s)
- ✅ 33GB の root `.db.bak*` 削除

### 2.3 次の deploy で自動反映されるもの (まだ走ってない)

- **migration 120** (`scripts/migrations/120_drop_dead_vec_unifts.sql`) — 30 個の DEAD vec/uni-FTS テーブルを DROP。entrypoint.sh §4 が `target_db: autonomath` を見て idempotent に走る。9.4GB → 7.5GB に縮小予想。

---

## 3. user 行動が必要なもの (代行不可)

| 項目 | 何を | どこ |
|---|---|---|
| outreach メール 5 通送信 | OTHER CLI の `analysis_wave18/consultant_firms_2026-04-30.csv` から 5 件選択、`research/outreach/templates_2026-04-30.md` の 5 テンプレ ① 〜 ⑤ を差し替えて GUI メールで送信 | 1 日 5 通まで、BCC 禁止、reply-to は info@bookyou.net |
| Smithery dashboard claim | https://smithery.ai に GitHub login → autonomath-mcp を claim (任意) | 配信導線追加 |
| Glama auto-index 待ち | passive、URL 修正後 24h で indexed されるはず | 今日 fix 済み、明日確認 |

これ以外は CLI 側で代行できる (実証済)。

---

## 4. OTHER CLI loop の状態

`research/loops/runs/20260430/` の loop が **`present=44/44 missing=0` で完了済**。次の自動アクション = `dedupe_and_deepen`。

期待 deliverable (各々が空でないかは未確認):

- `analysis_wave18/url_liveness_2026-04-30.json`
- `analysis_wave18/consultant_firms_2026-04-30.csv` ← outreach の入口
- `analysis_wave18/pdf_extraction_research_2026-04-30.md`
- `analysis_wave18/am_diff_design_2026-04-30.md`

次セッションでやること:
1. 上記 4 ファイルを Read で確認、品質チェック
2. consultant_firms.csv が 100 行以上あるか
3. pdf_extraction_research / am_diff_design の設計書を読み、私の CLI で parser / cron 実装フェーズに移る

---

## 5. KPI 状態 (LR plan §18)

朝 1 GREEN / 2 YELLOW / 12 RED → 夜:

| 領域 | 状態 |
|---|---|
| 公開数値 | 🟡 (manifest drift checker を回した上で残 ~30 issue、tool_count 93 で再 sync 必要) |
| 無料枠 | ✅ GREEN (3/日 統一済) |
| citation | 🟡 (programs/laws/case_studies で field 名 drift) |
| freshness | 🟡 (`fetched_at` と `verified_at` の分離未) |
| checksum | 🟡 (laws full、programs 61%、case_studies 0%) |
| provenance | 🔴 (`am_entity_facts.source_id` 18.3% — backfill 未) |
| diff | 🔴 (`am_amendment_diff` 0 行 — OTHER CLI の design が来たので実装フェーズへ) |
| audit seal | ✅ GREEN (10 paid endpoint で wired、verify endpoint live) |
| billing | 🔴 (usage_events 0 行 — 実顧客 0) |
| batch | 🟡 (bulk_evaluate に evidence_packet_endpoint 同梱済、CSV まだ) |
| token_cost | ✅ GREEN (token_compression 服用、bench harness 公開) |
| webhook | ✅ GREEN (diff_id + evidence_packet_endpoint 含む payload) |
| UI | ✅ GREEN (dark mode guard 入った、新画面 0 件) |
| legal | ✅ GREEN (一次資料100% / 必ず / 業界最安 全削除) |
| revenue | 🔴 (実 paid 0 件) |

GREEN 7 / YELLOW 4 / RED 4 — 朝の 1G/2Y/12R から大きく改善。

残 RED の半分は **OTHER CLI の deliverable + outreach 反応** 待ち。

---

## 6. 既知の issue / 要監視

### 6.1 Fly NRT volume 不安定

今日 deploy 中に `vol_4ojk82zk7xzeqxpr is on an unreachable host` の transient error が 1 回発生。machine 自動復旧。同様の事象が複数回起きるなら別 region / volume migration を検討 (failure_modes_2026-10-30.md #11 参照)。

### 6.2 `data/autonomath.db` footgun

`data/autonomath.db` は **0-byte placeholder**、本物は repo root の `autonomath.db` (9.4GB)。新コードが慣例で `data/autonomath.db` に書き込むと silent loss。**DIRECTORY.md に注意書き済**。

### 6.3 server.json description 中の tool count

`server.json` description は historically **93-tool** runtime と整合済 (旧 89 表記から 93 へ更新済)。次回 PyPI publish 時に pyproject.toml と server.json の version も bump 必要。

### 6.4 response envelope テスト 5 失敗

`test_response_envelope.py::test_houjin_404_with_v2_opt_in` 等が audit_seal embed の影響で 404 → 422 に変わってる。test 側の期待値を 422 に直す必要あり (1h 作業)。

### 6.5 funding_stack_checker REST テスト 3 失敗

`test_funding_stack_checker.py` の REST テストが 404 返しているのは、router mount は今日完了したが、テスト fixture が古い app instance を使っている可能性。次セッションで再走確認。

---

## 7. 次セッション 1 番目に見るべき 3 つ

1. **`analysis_wave18/consultant_firms_2026-04-30.csv`** — Read で内容確認。100 行以上あるか、連絡先 channel 1 つ以上あるか。あれば `research/outreach/templates_2026-04-30.md` の 5 通を差し替えて user 送信用 markdown 生成。
2. **`analysis_wave18/pdf_extraction_research_2026-04-30.md`** — OTHER CLI が出した PDF 構造調査。これを読んで `scripts/cron/extract_program_facts.py` (新規) の実装に着手。
3. **Fly machine status** — `flyctl status -a autonomath-api` で v40+ 健康確認、`/v1/meta/corpus_snapshot` 確認、migration 120 が適用されたか (DB が 7.5GB 程度に縮んだか) で判断。

---

## 8. 触ってはいけない (現時点)

- `research/loops/`、`research/_archive/` — OTHER CLI 運用空間
- `data/snapshots/`、`data/structured_facts/` — OTHER CLI 出力先
- `scripts/migrations/*.sql` の番号 0xx (immutable history)
- `data/autonomath.db` (0-byte placeholder — 触ると footgun)
- ライブの `autonomath.db` (root) を VACUUM 等のロック作業で触らない (OTHER CLI 並列読み)

---

## 9. 数字スナップショット

- Repo size (excluding .venv, .git): **~14 GB** (autonomath.db が支配的)
- Total routes: 203
- MCP tools at default gates: 93
- Programs (searchable): 11,684
- Adoption→program join: 72,056 exact + 9,162 fuzzy / 201,845 = 40% matched
- am_amendment_diff rows: 0 (OTHER CLI design 待ち)
- usage_events rows: 0 (paid 顧客 0)
- 公開 SEO pages: 1,416 + cross 47 + qa 23 + industries 22 + prefectures 48
- 30 day gate fires at 2026-06-05 if paid<3 OR pilots<3 (per LR plan §28.7 — まだ outreach 未送信なので clock 未開始)

---

## 10. 連絡先 / 認証

- Bookyou株式会社 T8010001213708, 代表 梅田茂利, info@bookyou.net
- GitHub: shigetosidumeda-cyber, repo `shigetosidumeda-cyber/autonomath-mcp`
- Cloudflare account `037691739017ffe105f57fe391f4aebb` (wrangler OAuth — 期限 2026-04-30 08:52 UTC、毎日 refresh)
- PyPI token (有効、2024-04-30 投入): `pypi-AgEIcHlwaS5vcmcCJG...` (.pypirc 化していない、毎回 inline)
- Fly app: `autonomath-api`, region `nrt`, volume `vol_4ojk82zk7xzeqxpr`
- `/tmp/mcp-publisher` v1.7.6 インストール済 (mac arm64)

以上。次セッションは `analysis_wave18/consultant_firms_2026-04-30.csv` を最初に開けば 5 分で復帰可能。
