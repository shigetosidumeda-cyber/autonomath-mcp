---
name: jpcite_db_sync_runbook
alias_of: autonomath_db_sync_runbook
brand_layer: jpcite
legacy_brand: autonomath
wave: 46.D
generated_at: 2026-05-12
status: alias (banner-only; canonical content unchanged)
---

# jpcite db sync runbook (alias of autonomath_db_sync_runbook)

> **Alias notice (Wave 46.D, 2026-05-12)**
>
> This document is the **jpcite-branded alias** of the canonical operator runbook
> previously published under the AutonoMath brand. As part of the Wave 46
> brand-consolidation pass (zeimu-kaikei.ai / AutonoMath → **jpcite**), this
> file points readers from the new brand surface to the unchanged canonical
> source. **No content has been migrated or rewritten** — see the source
> document for the authoritative, current operator procedure.
>
> The original file is preserved at its original path under the legacy
> brand name to keep historical links, search-engine references and
> bookmark URLs working (per
> `feedback_destruction_free_organization` — rm/mv 禁止, banner+index で整理).
>
> - **Canonical source (unchanged)**:
>   [`autonomath_db_sync_runbook.md`](./autonomath_db_sync_runbook.md)
> - **Brand history note**: the legacy brand marker is kept intentionally
>   minimal (per `feedback_legacy_brand_marker` — do not surface the old
>   brand prominently; this is an internal SEO-citation bridge only).
> - **Redirect index**:
>   [`REDIRECT_MAP_w46_canonical.md`](./REDIRECT_MAP_w46_canonical.md)

## How to read this alias

1. Open the canonical source linked above — that is the single source of truth.
2. The file you are reading right now is **discovery-only**: it exists so
   that grep / sitemap crawl / AI-agent retrieval that look up
   `jpcite_db_sync_*` resolve to a real markdown asset and find their way to
   the canonical content in one hop.
3. If you find yourself wanting to edit *this* file to change procedure,
   STOP — edit the canonical source instead. Aliases are diff-frozen.

## Scope summary (copied from canonical source for grep-discoverability only)

The canonical document covers, in this order:

- When to run the production sync (post batch-write, pre-launch milestones,
  post-migration of `am_*` tables).
- Local pre-flight checks (`autonomath.db` integrity, size budget against
  the 20 GB Fly volume, schema migration version).
- Transfer mechanics (compressed upload to Fly volume `/data/autonomath.db`,
  rename-with-fsync, restart of `autonomath-api` machine).
- Post-sync verification (row counts, machine health check, smoke probes).
- Rollback (previous `.db` snapshot kept on volume, fast atomic swap back).

**Do not** treat the bullets above as authoritative — they exist only so
that an agent or an operator searching by phrase finds the alias and
follows the link to the real runbook.

## Why an alias instead of a rename

- `rm` / `mv` is forbidden under `feedback_destruction_free_organization` —
  destruction-free organization rule (banner + index, never delete).
- The canonical filename is still load-bearing for: prior PR descriptions,
  older release notes, external mirrors (`web.archive.org`,
  `gist.github.com`), and internal bookmark URLs that pre-date Wave 46.
- The brand-consolidation goal (jpcite-discoverability) is fully served
  by adding a *new* alias surface — the legacy surface incurs zero
  rewrite cost and zero linkrot risk.

## Verification

- This alias was generated by **Wave 46.D** (`tick5#5`) on 2026-05-12.
- The canonical source byte-content is unchanged (verified by
  `tests/test_w47d_docs_canonical_alias.py`).
- See `docs/_internal/REDIRECT_MAP_w46_canonical.md` for the full
  Wave 46.D alias table.
