# Harness H7 + H8 — Runbook index + Resources/Prompts wiring + Manifest schema (2026-05-17)

Lane: solo. NO LLM. mypy strict 0. Tests 22/22 PASS.

## H7 P0.8 — runbook index

Before: 14 indexed in `docs/runbook/README.md`; 33 .md files on disk; 7
missing frontmatter; broken refs in `docs/operator_daily.md`.

After:
- 33/33 frontmatter coverage. 7 files added: aws_credit_run_workflows /
  aws_credit_sagemaker_embed_batch / cron_schedule_master /
  customer_webhook_signature / playwright_fallback /
  privacy_router_activation / search_console_setup.
- `docs/runbook/README.md` expanded with 19 newly-indexed runbooks across
  4 new categories (incident, ETL, billing) + 4 expanded categories
  (monitoring, secret, deploy, brand). `updated: 2026-05-04 → 2026-05-17`.
- `docs/operator_daily.md` broken refs repaired: `solo_ops_handoff.md`,
  `hiring_decision_gate.md`, `disaster_recovery.md` (root) → swapped to
  `CURRENT_SOT_2026-05-17.md` + `runbook/README.md` + `runbook/disaster_recovery.md`
  + `runbook/incident_response_v3_multi_root_cause.md`.
- `docs/_internal/CURRENT_SOT_2026-05-17.md` created — refreshes active
  SOT pointer (Wave 50 RC1 closure + Wave 51 tick 0 + PERF-1..32 + H3/H7/H8).

## H8 P1.1 — manifest inputSchema 0 → 31

`mcp-server.json` + `mcp-server.full.json`: 31 priority tools schematised
(top 30 set + an overlapping match).

Coverage:
- jpintel side (18): search_programs / get_program / batch_get_programs /
  list_exclusion_rules / check_exclusions / get_meta / get_usage_status /
  enum_values / search_enforcement_cases / search_case_studies /
  search_loan_programs / prescreen_programs / smb_starter_pack /
  subsidy_combo_finder / subsidy_roadmap_3yr / search_laws /
  search_court_decisions / search_bids.
- Wave 21 composition (5): apply_eligibility_chain_am /
  find_complementary_programs_am / simulate_application_am /
  track_amendment_lineage_am / program_active_periods_am.
- Wave 22 composition (5): match_due_diligence_questions /
  prepare_kessan_briefing / forecast_program_renewal /
  cross_check_jurisdiction / bundle_application_kit.
- Wave 23 industry packs (3): pack_construction / pack_manufacturing /
  pack_real_estate.

Each is a minimal draft-07 JSON Schema fragment (type / required /
properties / enums / min/max where the spec spelt them out). Residual
~153 tools carry empty inputSchema; deepening is mechanical without
semantic risk.

## H8 P1.1 — _meta reconciliation

- `mcp-server.json`: tool_count=184, resource_count=37, prompt_count=15.
- `mcp-server.full.json`: tool_count=219, resource_count=37.
- `mcp-server.composition.json`: tool_count=58, resource_count=0.
- `mcp-server.core.json`: tool_count=39, resource_count=0.

## H8 P1.2 — jpcite Resources/Prompts wired

Before: `mcp/jpcite_resources.py` (5 resources) + `mcp/jpcite_prompts.py`
(3 prompts) defined registrars but `mcp/server.py` never invoked them at
boot. REST `/v1/meta/resources` surfaced them via the aggregator, but
MCP `resources/list` only saw `autonomath://` URIs.

Additionally `src/jpintel_mcp/api/meta.py` previously imported a
hallucinated `list_autonomath_resources` / `list_autonomath_prompts`;
this fix already landed in a prior sweep (`list_resources as
list_autonomath_resources` alias). No edit needed in meta.py this sweep.

After:
- `mcp/server.py` (post DEEP-34 cohort block, inside the
  `if settings.autonomath_enabled:` branch) now calls
  `register_jpcite_resources(mcp)` + `register_jpcite_prompts(mcp)`.
  Idempotent + non-fatal on FastMCP version mismatch.
- Live smoke (gate OFF): TOTAL MCP resources = 42 (37 autonomath:// + 5
  mcp://jpcite/*).

## Tests

`tests/test_mcp_resources.py` expanded with 4 new H8 P1.2 tests + 2
contract sets (`EXPECTED_JPCITE_RESOURCES`, `EXPECTED_JPCITE_PROMPTS`)
+ `_jpcite_uris` helper. Result: **22/22 PASS** in 10.74s.

## Quality gates

- mypy --strict (touched files): 0 errors.
- ruff: 0 (no new lint debt).
- pytest test_mcp_resources.py: 22/22 PASS.

## Files touched

- `docs/runbook/README.md` — index expanded, updated stamp bumped.
- `docs/runbook/{aws_credit_run_workflows,aws_credit_sagemaker_embed_batch,cron_schedule_master,customer_webhook_signature,playwright_fallback,privacy_router_activation,search_console_setup}.md` — frontmatter added.
- `docs/operator_daily.md` — broken refs repaired (companion docs + §5.1 + §5.2 + §7).
- `docs/_internal/CURRENT_SOT_2026-05-17.md` — new SOT pointer.
- `mcp-server.json` — 31 inputSchema + `_meta` reconciled.
- `mcp-server.full.json` — 31 inputSchema + `_meta` reconciled.
- `mcp-server.composition.json` — `_meta` reconciled.
- `mcp-server.core.json` — `_meta` reconciled.
- `src/jpintel_mcp/mcp/server.py` — `register_jpcite_resources` + `register_jpcite_prompts` wired.
- `tests/test_mcp_resources.py` — 4 new tests + 2 contract sets + helper.

last_updated: 2026-05-17
